# WebScan AI Security Platform — 作品设计与实现

---

## 目录

0. [双系统架构设计概述](#0-双系统架构设计概述)
   - 0.1 架构演进背景与设计理念
   - 0.2 双系统全景架构图
   - 0.3 旧版系统架构（Classic WebScan）
   - 0.4 新版系统架构（TOSKill）
   - 0.5 双系统技术栈对比
   - 0.6 系统间依赖关系与数据交互
   - 0.7 部署架构与端口规划
   - 0.8 架构演进路线图
   - 0.9 双系统共存过渡策略
   - 0.10 开发、测试、部署差异化管理
   - 0.11 数据迁移方案与未来整合规划
   - 0.12 本章小结
1. [系统总体设计](#1-系统总体设计)
   - 1.1 设计目标与原则
   - 1.2 系统总体架构
   - 1.3 前端架构设计
   - 1.4 后端架构设计
   - 1.5 通信架构设计
   - 1.6 本章小结
2. [功能模块设计](#2-功能模块设计)
   - 2.1 模块总体划分
   - 2.2 信息收集模块
   - 2.3 漏洞扫描模块
   - 2.4 POC验证模块
   - 2.5 AI Agent工作流模块
   - 2.6 报告生成模块
   - 2.7 脚本管理模块
   - 2.8 前端页面模块
   - 2.9 本章小结
3. [数据库设计](#3-数据库设计)
   - 3.1 实体关系设计
   - 3.2 核心数据表结构
   - 3.3 本章小结
4. [接口设计](#4-接口设计)
   - 4.1 REST API设计
   - 4.2 WebSocket消息协议设计
   - 4.3 本章小结
5. [核心功能实现](#5-核心功能实现)
   - 5.1 AI Agent工作流引擎实现
   - 5.2 RAG知识库增强检索实现
   - 5.3 插件执行引擎实现
   - 5.4 WebSocket实时通信实现
   - 5.5 脚本安全审查实现
   - 5.6 本章小结
6. [关键技术实现](#6-关键技术实现)
   - 6.1 LangGraph多子图协同
   - 6.2 LLM增强动态任务规划
   - 6.3 认证信息管理
   - 6.4 前端状态管理与可靠性
   - 6.5 设计模式应用
   - 6.6 本章小结
7. [系统部署](#7-系统部署)
   - 7.1 部署架构设计
   - 7.2 后端部署流程
   - 7.3 前端部署流程
   - 7.4 本章小结
8. [系统界面展示](#8-系统界面展示)
   - 8.1 旧版系统界面
   - 8.2 新版系统界面
   - 8.3 本章小结

附录

---

## 0. 双系统架构设计概述

### 0.1 架构演进背景与设计理念

WebScan AI Security Platform 经历了从传统Web扫描平台到AI驱动智能扫描平台的架构演进，形成了**旧版系统（Classic WebScan）**与**新版系统（TOSKill）**双系统并存的格局。

#### 演进背景

| 阶段 | 时间 | 特征 | 核心痛点 |
|------|------|------|---------|
| **第一阶段** | 项目初期 | 构建 Classic WebScan（`front/` + `backend/`），实现完整的Web扫描功能 | 扫描流程固定，依赖人工配置，缺乏智能化 |
| **第二阶段** | AI技术引入 | 在Classic后端中集成 LangGraph AI Agent（`backend/ai_agents/`），实现初步智能化 | AI模块与旧有架构耦合，扩展困难 |
| **第三阶段** | 架构重构 | 独立构建 TOSKill（`TOSKill/` + `toskill-frontend/`），全新AI驱动架构 | 两套系统并行维护，需要过渡策略 |

#### 设计理念

双系统架构遵循 **"演进式重构（Evolutionary Refactoring）"** 策略：

- **旧系统稳定运行**：Classic WebScan 承载完整的扫描功能（信息收集、漏洞扫描、POC验证、AWVS集成、报告管理），作为生产环境主力
- **新系统创新探索**：TOSKill 以AI原生理念重新设计，采用WebSocket驱动的实时交互模式，探索AI Agent自主扫描的边界
- **渐进式迁移**：通过后端路由统一注册（`backend/api/__init__.py` 同时挂载两套系统的API路由），实现用户无感知的渐进过渡

#### 战略目标

| 目标 | 说明 | 当前状态 |
|------|------|---------|
| 功能完整性 | 旧系统覆盖全部扫描场景，新系统聚焦AI智能扫描 | 已完成 |
| 技术先进性 | 新系统验证LangGraph中断交互、RAG增强等前沿技术 | 进行中 |
| 平滑迁移 | 逐步将旧系统功能迁移到新系统架构 | 规划中 |
| 最终统一 | 以TOSKill架构为基础，融合旧系统成熟功能 | 远期目标 |

### 0.2 双系统全景架构图

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          WebScan AI Security Platform                                  │
│                              双系统并行架构全景图                                        │
│                                                                                       │
│  ┌─────────────────────────────────────┐    ┌─────────────────────────────────────┐  │
│  │       旧版系统 (Classic WebScan)      │    │        新版系统 (TOSKill)             │  │
│  │       成熟稳定 · 功能全面 · 生产主力    │    │       AI原生 · 实时交互 · 创新探索     │  │
│  │                                     │    │                                     │  │
│  │  ┌───────────────────────────────┐  │    │  ┌───────────────────────────────┐  │  │
│  │  │  front/ (Vue 3 + Element Plus)│  │    │  │  toskill-frontend/ (Vue 3)    │  │  │
│  │  │  • 15个页面视图                │  │    │  │  • 5个功能视图                  │  │  │
│  │  │  • 13个业务组件                │  │    │  │  • Agent工作空间                │  │  │
│  │  │  • Pinia状态管理(4 Store)      │  │    │  │  • 单文件响应式Store             │  │  │
│  │  │  • 4个WebSocket连接            │  │    │  │  • 1个WebSocket连接             │  │  │
│  │  │  • 监听 :5173 ▸代理→ :8888     │  │    │  │  • 监听 :5173 ▸代理→ :8081     │  │  │
│  │  └───────────────┬───────────────┘  │    │  └───────────────┬───────────────┘  │  │
│  │                  │                  │    │                  │                  │  │
│  │  ┌───────────────┴───────────────┐  │    │  ┌───────────────┴───────────────┐  │  │
│  │  │  backend/ (FastAPI :8888)      │  │    │  │  TOSKill/ (FastAPI :8081)      │  │  │
│  │  │  • 17个API路由模块             │  │    │  │  • 6个API路由模块               │  │  │
│  │  │  • Tortoise-ORM (14张表)       │  │    │  │  • 内存状态 + SQLite            │  │  │
│  │  │  • 任务执行器+状态机           │  │    │  │  • LangGraph工作流引擎           │  │  │
│  │  │  • 14个信息收集插件            │  │    │  │  • 11个信息收集工具              │  │  │
│  │  │  • 9个漏洞扫描器               │  │    │  │  • 8个漏洞扫描工具               │  │  │
│  │  │  • 14个POC脚本                 │  │    │  │  • 3个POC验证工具                │  │  │
│  │  │  • AWVS集成 + Seebug集成       │  │    │  │  • RAG知识库(14个文档)           │  │  │
│  │  │  • AI Agents (LangGraph子模块)  │◄─┼────┼──│  • 用户交互中断(interrupt)       │  │  │
│  │  │  • 多进程插件执行器             │  │    │  │  • 认证信息共享机制              │  │  │
│  │  └───────────────────────────────┘  │    │  └───────────────────────────────┘  │  │
│  │                                     │    │                                     │  │
│  │  数据存储: SQLite (webscan.db)       │    │  数据存储: SQLite (toskill.db)       │  │
│  │  日志: logs/app.log                 │    │  日志: logs/toskill.log              │  │
│  └─────────────────────────────────────┘    └─────────────────────────────────────┘  │
│                                                                                       │
│  ┌─────────────────────────────────────────────────────────────────────────────────┐  │
│  │                              共享基础设施层                                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │  │
│  │  │ 工具实现共享   │  │  LLM API密钥  │  │  POC脚本库    │  │  RAG知识文档          │ │  │
│  │  │ (TOSKill/tools │  │  (统一配置)   │  │  (backend/poc) │  │  (TOSKill/RAG/knowledge)│ │  │
│  │  │  被两系统引用)  │  │              │  │               │  │                       │ │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 0.3 旧版系统架构（Classic WebScan）

#### 技术栈选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| **前端框架** | Vue 3 (Composition API) | 3.5.24 | 响应式数据绑定，组件化开发，生态成熟 |
| **构建工具** | Vite | 5.4.0 | 极速HMR，ESBuild预构建，Tree-shaking |
| **UI组件库** | Element Plus | 2.8.0 | 企业级组件库，中文文档完善，社区活跃 |
| **状态管理** | Pinia | 2.3.0 | Vue 3官方推荐，TypeScript友好，模块化Store |
| **路由** | Vue Router | 4.5.24 | SPA路由管理，导航守卫，滚动恢复 |
| **HTTP客户端** | Axios | 1.7.9 | 拦截器机制，请求/响应转换，取消请求 |
| **图表** | Chart.js | 4.4.0 | 轻量级Canvas图表，8种图表类型 |
| **后端框架** | FastAPI | latest | 异步高性能，自动API文档，Pydantic校验 |
| **ORM** | Tortoise-ORM | 0.21.6 | 异步ORM，支持SQLite/PostgreSQL，类Django API |
| **数据库** | SQLite | - | 零配置，文件级存储，适合单机部署 |

#### 架构分层

```
┌────────────────────────────────────────────────────────────────┐
│                    旧版系统分层架构                               │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                  展示层 (front/)                          │ │
│  │  Vue 3 + Element Plus + Pinia + Vue Router + Chart.js    │ │
│  │  15个页面视图 · 13个业务组件 · 6个通用组件 · 16个工具模块    │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │ HTTP REST + WebSocket              │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  路由层 (backend/api/)                    │ │
│  │  17个API模块 · 统一路由注册 · 日志中间件 · 请求验证        │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  业务层                                    │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │ 任务执行器    │  │ AI Agents    │  │ 服务层        │   │ │
│  │  │ (task_executor│  │ (ai_agents/) │  │ (services/)   │   │ │
│  │  │  ~2500行)    │  │ 4个子图+工具 │  │ 通知+报告     │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  执行层                                    │ │
│  │  ┌────────────────┐  ┌────────────────┐                  │ │
│  │  │ 插件执行器      │  │ 漏洞扫描管理器  │                  │ │
│  │  │ (多进程隔离)    │  │ (模板方法模式)  │                  │ │
│  │  └────────────────┘  └────────────────┘                  │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  数据层                                    │ │
│  │  Tortoise-ORM + SQLite · 14张数据表 · 状态机 · 迁移工具   │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

#### 核心模块

| 模块 | 路径 | 功能 | 关键文件 |
|------|------|------|---------|
| API路由 | `backend/api/` | 17个路由模块，统一注册 | `__init__.py`（路由注册中心） |
| 数据模型 | `backend/models.py` | 14张数据表，状态机，严格转换校验 | `models.py` |
| 任务执行器 | `backend/task_executor.py` | 核心调度引擎，~2500行 | `task_executor.py` |
| 插件执行器 | `backend/plugin_executor.py` | 多进程隔离，心跳监控 | `plugin_executor.py` |
| AI Agents | `backend/ai_agents/` | LangGraph工作流，4子图协同 | `core/graph.py` |
| 信息收集插件 | `backend/plugins/` | 14个独立插件 | 各插件目录 |
| 漏洞扫描器 | `backend/vulnerability_scan_plugins/` | 9个扫描器，模板方法 | `manager.py` |
| POC系统 | `backend/poc/` + `backend/ai_agents/poc_system/` | 14个POC脚本，7大框架 | 各POC目录 |
| AWVS集成 | `backend/AVWS/` | 8个API类封装 | `API/Base.py` |
| 前端页面 | `front/src/views/` | 15个页面视图 | 各Vue组件 |
| 前端工具 | `front/src/utils/` | 16个工具模块 | `api.js`, `websocket.js` 等 |

### 0.4 新版系统架构（TOSKill）

#### 技术栈选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| **前端框架** | Vue 3 (Composition API) | 3.5.32 | 与旧系统技术栈一致，降低学习成本 |
| **构建工具** | Vite | 8.0.10 | 最新版本，更快的构建速度 |
| **UI方案** | 轻量级原生CSS + Lucide Icons | 1.0.0 | 追求极致轻量，无重型UI库依赖 |
| **状态管理** | Reactive单文件Store | - | 场景简单，无需Pinia复杂度 |
| **Markdown渲染** | marked | 18.0.3 | AI对话内容Markdown渲染 |
| **后端框架** | FastAPI | latest | 与旧系统一致，路由可统一注册 |
| **AI工作流** | LangGraph | latest | 图状态机，支持interrupt中断交互 |
| **LLM框架** | LangChain | latest | LLM调用抽象，工具集成 |
| **RAG框架** | LlamaIndex | latest | 向量索引，知识检索增强 |
| **数据库** | SQLite (可选) | - | 轻量持久化，可选启用 |

#### 架构分层

```
┌────────────────────────────────────────────────────────────────┐
│                    新版系统分层架构                               │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │               展示层 (toskill-frontend/)                  │ │
│  │  Vue 3 + Vite · 轻量级CSS · 单文件Store · WebSocket驱动  │ │
│  │  5个功能视图 · Agent工作空间 · 浮动日志面板                 │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │ HTTP REST + WebSocket (主力)       │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  路由层 (TOSKill/api/)                    │ │
│  │  6个API模块 · WebSocket端点 · 扫描/报告/对话/脚本/日志    │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  AI核心层 (TOSKill/AI/)                   │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │ graph.py     │  │ state.py     │  │ tools.py      │   │ │
│  │  │ 3个子图工作流 │  │ ScanState    │  │ 22个安全工具   │   │ │
│  │  │ Agent编排器  │  │ TypedDict    │  │ 统一ToolResult │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │ core.py      │  │ llm_client.py│  │ validators.py │   │ │
│  │  │ 核心业务逻辑  │  │ LLM客户端    │  │ 输入验证器    │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  RAG知识层 (TOSKill/RAG/)                 │ │
│  │  rag_engine.py · retriever.py · 14个知识文档              │ │
│  │  HuggingFace嵌入 (BAAI/bge-small-zh-v1.5) · 向量存储     │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐ │
│  │                  工具层 (TOSKill/tools/)                   │ │
│  │  信息收集(11) · 漏洞扫描(8) · POC验证(3) · 报告生成(4)    │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

#### 核心模块

| 模块 | 路径 | 功能 | 关键特性 |
|------|------|------|---------|
| AI工作流 | `TOSKill/AI/graph.py` | 3子图工作流 + Agent编排器 | interrupt中断交互，用户确认机制 |
| AI状态 | `TOSKill/AI/state.py` | ScanState TypedDict定义 | 会话状态，认证信息，执行历史 |
| 工具注册 | `TOSKill/AI/tools.py` | 22个@tool装饰器注册 | 统一ToolResult格式，认证传递 |
| LLM客户端 | `TOSKill/AI/llm_client.py` | LLM连接管理与调用 | 可用性检测，回退策略 |
| 核心逻辑 | `TOSKill/AI/core.py` | 扫描核心业务逻辑 | 意图识别，工具调度 |
| 脚本安全 | `TOSKill/AI/script_safety.py` | 自定义脚本安全审查 | AST分析，恶意模式检测 |
| 模型检测 | `TOSKill/AI/model_check.py` | AI模型连接验证 | 启动时自动检测 |
| RAG引擎 | `TOSKill/RAG/rag_engine.py` | 知识库引擎 | LlamaIndex向量索引 |
| RAG检索 | `TOSKill/RAG/retriever.py` | 知识检索 | 工具-知识映射，上下文扩充 |
| WebSocket | `TOSKill/api/ai_chat_websocket.py` | WebSocket连接管理 | 会话管理，双向消息路由 |
| 扫描API | `TOSKill/api/scan_api.py` | REST扫描接口 | 单工具/批量/全流程扫描 |
| 日志WebSocket | `TOSKill/api/log_websocket.py` | 实时日志推送 | 扫描日志实时传输 |
| 报告API | `TOSKill/api/report.py` | 报告生成与下载 | Markdown格式报告 |
| 前端工作空间 | `toskill-frontend/src/components/AgentWorkspace/` | Agent交互界面 | ChatArea, CommandInput, HistoryRail |
| 前端视图 | `toskill-frontend/src/components/views/` | 5个功能视图 | Console, Scan, Tools, Reports, Settings |
| 前端服务 | `toskill-frontend/src/services/` | API + WebSocket服务 | 单例模式，自动重连 |
| 前端组合式 | `toskill-frontend/src/composables/` | 逻辑复用 | useAgentChat, useLogBus, useLoadingState |

### 0.5 双系统技术栈对比

| 对比维度 | 旧版系统 (Classic WebScan) | 新版系统 (TOSKill) |
|---------|--------------------------|-------------------|
| **前端框架** | Vue 3.5.24 | Vue 3.5.32 |
| **构建工具** | Vite 5.4.0 | Vite 8.0.10 |
| **UI方案** | Element Plus 2.8.0 (重型) | 原生CSS + Lucide Icons (轻量) |
| **状态管理** | Pinia 2.3.0 (4个Store) | 单文件Reactive Store |
| **路由** | Vue Router 4.5.24 (15个路由) | 无路由（单页视图切换） |
| **图表** | Chart.js 4.4.0 | 无 |
| **HTTP客户端** | Axios 1.7.9 (9大API模块) | 原生Fetch (单例ApiService) |
| **后端框架** | FastAPI (8888端口) | FastAPI (8081端口) |
| **ORM** | Tortoise-ORM 0.21.6 | 无ORM（内存状态 + 可选SQLite） |
| **数据库** | SQLite (webscan.db, 14张表) | SQLite (toskill.db, 可选) |
| **AI工作流** | LangGraph (4子图，backend/ai_agents/) | LangGraph (3子图，TOSKill/AI/) |
| **RAG** | 无独立RAG模块 | LlamaIndex (14个知识文档) |
| **交互模式** | REST API为主 + WebSocket辅助 | WebSocket为主 + REST辅助 |
| **用户交互** | 批量创建任务，异步执行 | 实时对话式交互，interrupt中断 |
| **外部集成** | AWVS + Seebug + Pocsuite3 | 无外部集成 |
| **插件系统** | 多进程隔离，14个插件 | 22个集成工具，@tool装饰器 |
| **认证机制** | 无 | 认证信息自动提取/加密/共享 |
| **前端文件数** | ~50个源文件 | ~25个源文件 |
| **后端文件数** | ~80个源文件 | ~35个源文件 |

### 0.6 系统间依赖关系与数据交互

#### 路由统一注册（关键集成点）

两套系统的后端API通过 `backend/api/__init__.py` 实现统一路由注册：

```python
# backend/api/__init__.py - 路由注册中心
api_router = APIRouter()

# 旧版系统路由 (Classic WebScan)
api_router.include_router(tasks.router, prefix="/tasks", tags=["任务管理"])
api_router.include_router(reports.router, prefix="/reports", tags=["报告管理"])
api_router.include_router(awvs.router, prefix="/awvs", tags=["AWVS漏洞扫描"])
api_router.include_router(poc.router, tags=["POC扫描"])
# ... 更多旧版路由

# 新版系统路由 (TOSKill) -- 挂载到同一api_router下
api_router.include_router(report_router, tags=["报告下载"])
api_router.include_router(ai_chat_router, tags=["AI对话WebSocket"])
api_router.include_router(scan_router, tags=["TOSKill扫描API"])
api_router.include_router(chat_router, tags=["TOSKill聊天API"])
```

#### 系统间依赖关系图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        系统间依赖关系                                 │
│                                                                     │
│  ┌──────────────────────┐          ┌──────────────────────┐        │
│  │  旧版前端 (front/)    │          │  新版前端 (toskill-   │        │
│  │  :5173 ▸代理→ :8888  │          │  frontend/)           │        │
│  │                      │          │  :5173 ▸代理→ :8081   │        │
│  └──────────┬───────────┘          └──────────┬───────────┘        │
│             │                                 │                     │
│             ▼                                 ▼                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              旧版后端 (backend/ :8888)                        │  │
│  │                                                              │  │
│  │  ┌──────────────────┐    ┌──────────────────────────────┐   │  │
│  │  │ 自有API模块 (17个) │    │ 挂载TOSKill路由 (4个模块)     │   │  │
│  │  │ /api/tasks       │    │ /api/scan/* (TOSKill扫描)     │   │  │
│  │  │ /api/reports     │    │ /api/ai-chat/* (TOSKill对话)  │   │  │
│  │  │ /api/awvs        │    │ /api/reports/* (TOSKill报告)  │   │  │
│  │  │ /api/agents      │    │ /api/chat/* (TOSKill聊天)     │   │  │
│  │  │ ...              │    │ /api/scripts/* (TOSKill脚本)  │   │  │
│  │  └──────────────────┘    └──────────────┬───────────────┘   │  │
│  │                                         │                    │  │
│  │  ┌──────────────────────────────────────┘                   │  │
│  │  │  旧版后端通过import引入TOSKill模块，共享同一进程空间       │  │
│  │  │  from TOSKill.api import ai_chat_router, scan_router    │  │
│  │  └─────────────────────────────────────────────────────────│  │
│  └──────────────────────────────────────────────────────────────┘  │
│             │                                                       │
│             │ 工具共享                                               │
│             ▼                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              新版后端 (TOSKill/ :8081)                        │  │
│  │                                                              │  │
│  │  ┌──────────────────┐    ┌──────────────────────────────┐   │  │
│  │  │ 独立运行模式      │    │ 被嵌入模式 (旧版后端引用)      │   │  │
│  │  │ python main.py   │    │ 作为Python模块被导入           │   │  │
│  │  │ 监听 :8081       │    │ 共享旧版后端的进程和端口        │   │  │
│  │  │ 独立前端访问      │    │ 旧版前端通过/api/*访问         │   │  │
│  │  └──────────────────┘    └──────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                       共享资源                                 │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐ │  │
│  │  │ TOSKill/tools/ │  │ RAG/knowledge/ │  │ LLM API配置    │ │  │
│  │  │ 工具实现代码    │  │ 知识文档       │  │ 模型密钥共享    │ │  │
│  │  │ (被两系统引用)  │  │ (被TOSKill使用) │  │ (.env配置)     │ │  │
│  │  └────────────────┘  └────────────────┘  └────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

#### 模块调用流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      AI Agent扫描 - 双系统调用流程                     │
│                                                                     │
│  旧版前端 (front/)                   新版前端 (toskill-frontend/)    │
│  ┌──────────────────┐               ┌──────────────────┐           │
│  │ AgentScan.vue    │               │ ConsoleView.vue  │           │
│  │ POST /api/agents │               │ WS /api/ai-chat  │           │
│  └────────┬─────────┘               └────────┬─────────┘           │
│           │                                   │                     │
│           ▼                                   ▼                     │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              旧版后端 (backend/ :8888)                      │    │
│  │                                                            │    │
│  │  ┌──────────────────┐    ┌────────────────────────────┐   │    │
│  │  │ /api/agents/*    │    │ /api/ai-chat/ws            │   │    │
│  │  │ (ai_agents路由)   │    │ (TOSKill WebSocket路由)    │   │    │
│  │  └────────┬─────────┘    └────────────┬───────────────┘   │    │
│  │           │                           │                    │    │
│  │           ▼                           ▼                    │    │
│  │  ┌──────────────────┐    ┌────────────────────────────┐   │    │
│  │  │ backend/ai_agents│    │ TOSKill/api/               │   │    │
│  │  │ /core/graph.py   │    │ ai_chat_websocket.py       │   │    │
│  │  │ 4子图LangGraph    │    │ AgentOrchestrator          │   │    │
│  │  └────────┬─────────┘    └────────────┬───────────────┘   │    │
│  │           │                           │                    │    │
│  │           │          ┌────────────────┘                    │    │
│  │           ▼          ▼                                     │    │
│  │  ┌────────────────────────────────────────────────────┐   │    │
│  │  │              共享工具层                              │   │    │
│  │  │  TOSKill/tools/ (info_collection + vuln_scan + poc) │   │    │
│  │  │  backend/plugins/ (14个插件)                         │   │    │
│  │  │  backend/vulnerability_scan_plugins/ (9个扫描器)     │   │    │
│  │  └────────────────────────────────────────────────────┘   │    │
│  └────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

### 0.7 部署架构与端口规划

```
┌─────────────────────────────────────────────────────────────────────┐
│                         部署架构图                                    │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                     Nginx 反向代理 (:80)                        │ │
│  │                                                                │ │
│  │  /              → 旧版前端静态文件 (front/dist/)                │ │
│  │  /api/*         → 旧版后端 (127.0.0.1:8888)                    │ │
│  │  /toskill/      → 新版前端静态文件 (toskill-frontend/dist/)     │ │
│  │  /ws            → 旧版后端 WebSocket (127.0.0.1:8888)          │ │
│  └───────────────────────────────────────────────────────────────┘ │
│         │                      │                                    │
│         ▼                      ▼                                    │
│  ┌──────────────────┐  ┌──────────────────────────────────────┐   │
│  │ 旧版前端静态文件   │  │ 旧版后端 (FastAPI + Uvicorn)          │   │
│  │ front/dist/      │  │ 127.0.0.1:8888                       │   │
│  │                  │  │                                      │   │
│  │ Vue 3 SPA        │  │ ┌────────────────────────────────┐  │   │
│  │ Element Plus     │  │ │ 内置TOSKill模块 (同一进程)       │  │   │
│  │ Pinia + Router   │  │ │ /api/scan/* → TOSKill扫描API    │  │   │
│  └──────────────────┘  │ │ /api/ai-chat/* → TOSKill对话API │  │   │
│                        │ └────────────────────────────────┘  │   │
│                        │                                      │   │
│                        │ 数据: data/webscan.db                 │   │
│                        │ 日志: logs/app.log                    │   │
│                        └──────────────────────────────────────┘   │
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────────────────────────┐   │
│  │ 新版前端静态文件   │  │ 新版后端 (独立模式, 可选)              │   │
│  │ toskill-frontend/ │  │ 127.0.0.1:8081                       │   │
│  │ dist/             │  │                                      │   │
│  │                   │  │ TOSKill独立运行                       │   │
│  │ Vue 3 SPA (轻量)  │  │ LangGraph + RAG                      │   │
│  │ Agent工作空间      │  │                                      │   │
│  └──────────────────┘  │ 数据: data/toskill.db                  │   │
│                        │ 日志: logs/toskill.log                 │   │
│                        └──────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

#### 端口规划

| 服务 | 端口 | 协议 | 说明 |
|------|------|------|------|
| Nginx | 80/443 | HTTP/HTTPS | 统一入口，反向代理 |
| 旧版后端 (backend) | 8888 | HTTP | FastAPI + Uvicorn，内置TOSKill路由 |
| 新版后端 (TOSKill) | 8081 | HTTP | 独立运行时监听 |
| 旧版前端开发服务器 | 5173 | HTTP | Vite Dev Server，代理→8888 |
| 新版前端开发服务器 | 5173 | HTTP | Vite Dev Server，代理→8081 |

### 0.8 架构演进路线图

```
┌─────────────────────────────────────────────────────────────────────┐
│                       架构演进路线图                                   │
│                                                                     │
│  阶段1 (已完成)        阶段2 (当前)          阶段3 (规划中)            │
│  Classic WebScan      双系统并行              TOSKill统一架构          │
│                                                                     │
│  ┌──────────────┐    ┌──────────────┐      ┌──────────────┐        │
│  │ front/       │    │ front/       │      │              │        │
│  │ + backend/   │    │ + backend/   │      │  TOSKill     │        │
│  │              │    │              │      │  统一平台     │        │
│  │ • 完整扫描    │    │ • 生产主力    │      │              │        │
│  │ • 无AI能力   │    │ • 稳定运行    │      │ • AI原生     │        │
│  └──────────────┘    │              │      │ • 全功能     │        │
│         │            │  ┌──────────────┐  │ • 统一架构   │        │
│         │            │  │ TOSKill/     │  │              │        │
│         ▼            │  │ + toskill-   │  └──────────────┘        │
│  ┌──────────────┐    │  │ frontend/    │        ↑                 │
│  │ 引入AI Agent │    │  │              │        │                 │
│  │ backend/     │    │  │ • AI探索     │        │                 │
│  │ ai_agents/   │    │  │ • 实时交互   │        │                 │
│  └──────────────┘    │  │ • 轻量架构   │        │                 │
│         │            │  └──────────────┘        │                 │
│         ▼            │        │                 │                 │
│  ┌──────────────┐    │        │    功能迁移     │                 │
│  │ 路由统一注册  │    │        └────────────────→                 │
│  │ TOSKill路由   │    │                                          │
│  │ 挂载到backend │    │  迁移策略:                                 │
│  └──────────────┘    │  1. AWVS集成 → TOSKill独立工具             │
│                      │  2. 报告系统 → 统一到TOSKill               │
│                      │  3. 插件系统 → 迁移到@tool模式              │
│                      │  4. 前端页面 → 逐步迁移到Agent工作空间      │
│                      │  5. 数据库 → 统一数据模型                   │
│                      └──────────────────────────────────────────  │
│                                                                     │
│  时间线: ──●────────────●────────────●────────────●────────────→   │
│        项目启动    AI Agent引入   TOSKill重构    双系统并行    统一架构 │
└─────────────────────────────────────────────────────────────────────┘
```

### 0.9 双系统共存过渡策略

#### 业务边界划分

两套系统在当前阶段有明确的业务边界划分：

| 业务场景 | 使用系统 | 原因 |
|---------|---------|------|
| **传统Web扫描**（信息收集→漏洞扫描→POC验证→报告） | 旧版系统 | 功能完整，流程成熟，生产验证 |
| **AWVS集成扫描** | 旧版系统 | AWVS集成仅在旧版后端实现 |
| **Seebug知识库搜索** | 旧版系统 | Seebug集成仅在旧版后端实现 |
| **批量任务管理** | 旧版系统 | 完善的任务队列和状态机 |
| **AI Agent智能扫描** | 双系统均可 | 旧版通过ai_agents模块，新版通过TOSKill |
| **AI对话式扫描** | 新版系统 | WebSocket实时交互，interrupt中断确认 |
| **RAG知识增强** | 新版系统 | RAG模块仅在TOSKill中实现 |
| **自定义脚本上传** | 双系统均可 | 旧版通过脚本管理，新版通过script_router |
| **报告生成与导出** | 双系统均可 | 各自独立的报告生成模块 |

#### 用户路由策略

```
用户请求
    │
    ├── 使用旧版前端 (front/)
    │   ├── /scan-tasks → 旧版任务管理（完整功能）
    │   ├── /agent-scan → 旧版AI Agent扫描（ai_agents模块）
    │   ├── /awvs-scan → 旧版AWVS集成扫描
    │   ├── /poc-scan → 旧版POC扫描
    │   └── /api/ai-chat/* → 转发到TOSKill模块（嵌入模式）
    │
    └── 使用新版前端 (toskill-frontend/)
        ├── / → ConsoleView（AI对话式扫描）
        ├── /scan → ScanView（TOSKill扫描）
        ├── /tools → ToolsView（TOSKill工具列表）
        ├── /reports → ReportsView（TOSKill报告）
        └── /settings → SettingsView（TOSKill设置）
```

### 0.10 开发、测试、部署差异化管理

#### 开发流程差异

| 维度 | 旧版系统 | 新版系统 |
|------|---------|---------|
| **前端开发启动** | `cd front && npm run dev` | `cd toskill-frontend && npm run dev` |
| **后端开发启动** | `cd backend && python -m backend.main` | `cd TOSKill && python main.py` |
| **依赖管理** | `backend/requirements.txt` + `front/package.json` | `TOSKill/requirements.txt` + `toskill-frontend/package.json` |
| **代码规范** | ESLint + Pytest | 无Lint配置（轻量级） |
| **热重载** | Vite HMR + Uvicorn reload | Vite HMR + Uvicorn reload |
| **API文档** | Swagger UI (:8888/docs) | Swagger UI (:8081/docs) |

#### 测试策略差异

| 维度 | 旧版系统 | 新版系统 |
|------|---------|---------|
| **测试框架** | Pytest | Pytest |
| **测试覆盖** | API测试 + WebSocket测试 + 单元测试 | 工作流测试 + WebSocket测试 + API测试 + 工具测试 + 压力测试 + RAG测试 + 脚本安全测试 |
| **测试文件数** | 6个测试文件 | 8个测试文件 |
| **Mock策略** | 前端有Mock API/WebSocket | 无前端Mock |
| **CI/CD** | 可配置 | 可配置 |

#### 部署流程差异

| 维度 | 旧版系统 | 新版系统 |
|------|---------|---------|
| **前端构建** | `npm run build` → `front/dist/` | `npm run build` → `toskill-frontend/dist/` |
| **后端部署** | `uvicorn backend.main:app --port 8888` | `uvicorn TOSKill.main:app --port 8081` |
| **静态文件** | Nginx serve `front/dist/` | Nginx serve `toskill-frontend/dist/` |
| **反向代理** | Nginx `/api/*` → `:8888` | Nginx `/api/*` → `:8888`（嵌入模式）或 `:8081`（独立模式） |
| **进程管理** | Supervisor / systemd | Supervisor / systemd（独立模式） |
| **日志管理** | `logs/app.log` + `logs/api.log` | `logs/toskill.log` |

### 0.11 数据迁移方案与未来整合规划

#### 数据迁移方案

当前两套系统使用独立的数据库，迁移策略如下：

| 数据类别 | 当前存储 | 迁移方案 | 优先级 |
|---------|---------|---------|--------|
| 扫描任务记录 | 旧版 webscan.db | 导出为JSON，导入TOSKill统一存储 | 低 |
| 漏洞数据 | 旧版 Vulnerability表 | 建立统一漏洞模型，批量迁移 | 中 |
| 扫描报告 | 旧版 reports/ 目录 | 保持文件存储，统一报告格式 | 中 |
| 知识库 | 旧版 VulnerabilityKB表 | 合并到TOSKill RAG知识文档 | 高 |
| 系统配置 | 旧版 SystemSettings表 | 统一到TOSKill config.py | 低 |
| POC脚本 | 旧版 backend/poc/ | 统一到TOSKill/tools/poc/ | 中 |

#### 未来整合规划

```
┌─────────────────────────────────────────────────────────────────────┐
│                       未来整合目标架构                                │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              统一前端 (TOSKill-frontend 增强版)                  │ │
│  │                                                               │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │ │
│  │  │ AI对话    │  │ 任务管理  │  │ 报告中心  │  │ 系统设置  │     │ │
│  │  │ (主力交互) │  │ (原旧版)  │  │ (统一)   │  │ (统一)   │     │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                │                                    │
│  ┌─────────────────────────────┴─────────────────────────────────┐ │
│  │              统一后端 (TOSKill 增强版)                          │ │
│  │                                                               │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │ │
│  │  │ LangGraph    │  │ 任务调度器    │  │ 外部集成          │   │ │
│  │  │ AI工作流引擎  │  │ (状态机+队列) │  │ (AWVS/Seebug)    │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘   │ │
│  │                                                               │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │ │
│  │  │ 统一工具注册  │  │ RAG知识库    │  │ 统一数据模型      │   │ │
│  │  │ (@tool装饰器) │  │ (LlamaIndex) │  │ (Tortoise-ORM)   │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘   │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

**整合优先级：**
1. **高优先级**：统一工具注册（所有旧版插件迁移为@tool装饰器）、统一数据模型
2. **中优先级**：统一报告系统、前端页面整合
3. **低优先级**：外部集成迁移（AWVS、Seebug）、历史数据迁移

### 0.12 本章小结

本章详细阐述了 WebScan AI Security Platform 的双系统架构设计。项目采用"演进式重构"策略，在保留成熟稳定的 Classic WebScan 系统的同时，独立构建了AI原生的 TOSKill 系统。两套系统通过后端路由统一注册实现无缝集成，在工具层共享核心实现代码，在业务层各有侧重——旧系统负责完整的传统扫描流程和外部集成，新系统聚焦AI驱动的智能交互式扫描。通过明确的业务边界划分和渐进式迁移策略，确保双系统在过渡期内稳定共存，为未来的统一架构奠定基础。

---

## 1. 系统总体设计

### 1.1 设计目标与原则

WebScan AI Security Platform 的设计遵循以下核心原则：

| 设计原则 | 具体体现 | 实现方式 |
|---------|---------|---------|
| **智能化** | LLM驱动任务规划与决策 | LangGraph工作流引擎 + RAG知识库 |
| **模块化** | 插件化架构，热插拔工具 | 插件执行器 + 工具注册表 |
| **实时性** | 全双工实时通信 | WebSocket + 心跳 + 自动重连 |
| **可扩展性** | 动态工具注册，自定义脚本 | 脚本管理器 + 安全审查 |
| **可靠性** | 状态持久化，任务恢复 | 状态机 + 数据库持久化 + 重试机制 |
| **安全性** | 脚本安全审查，输入校验 | AST分析 + 危险模式检测 |
| **双轨并行** | 旧系统稳定运行 + 新系统创新探索 | 路由统一注册 + 渐进式迁移 |

### 1.2 系统总体架构

系统采用**前后端分离**的B/S架构，支持双系统并行部署。整体分为前端展示层、后端服务层、数据持久层三大层次。详细的双系统全景架构请参见第0.2节。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                  前端展示层 (双前端: front/ + toskill-frontend/)                │
│                                                                               │
│  ┌────────────────────────────────┐  ┌────────────────────────────────┐     │
│  │ 旧版前端 (Vue 3 + Element Plus) │  │ 新版前端 (Vue 3 + 轻量级CSS)    │     │
│  │ 15个页面 · Pinia · Router      │  │ 5个视图 · 单文件Store · 无Router │     │
│  │ Axios + 4个WebSocket连接       │  │ Fetch + 1个WebSocket连接        │     │
│  └───────────────┬────────────────┘  └───────────────┬────────────────┘     │
│                  │                                   │                       │
│                  │  HTTP REST / WebSocket             │                       │
│                  ▼                                   ▼                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │           后端服务层 (FastAPI + Python，双后端路由统一注册)              │    │
│  │                                                                       │    │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────┐     │    │
│  │  │ 旧版API (17个路由模块)     │  │ 新版API (6个路由模块，嵌入)    │     │    │
│  │  │ /api/tasks, /api/reports │  │ /api/scan, /api/ai-chat      │     │    │
│  │  │ /api/awvs, /api/agents   │  │ /api/reports, /api/chat      │     │    │
│  │  │ /api/poc, /api/kb ...    │  │ /api/scripts, /api/logs      │     │    │
│  │  └──────────────┬───────────┘  └──────────────┬───────────────┘     │    │
│  │                 │                              │                      │    │
│  │  ┌──────────────┴──────────────────────────────┴───────────────┐    │    │
│  │  │                    共享工具层                                │    │    │
│  │  │  TOSKill/tools/ + backend/plugins/ + backend/poc/           │    │    │
│  │  │  信息收集(14) · 漏洞扫描(9) · POC验证(14)                    │    │    │
│  │  └──────────────────────────────────────────────────────────────┘    │    │
│  └───────────────────────────────────┬─────────────────────────────────┘    │
│                                      │                                      │
│  ┌───────────────────────────────────┴─────────────────────────────────┐    │
│  │                        数据持久层                                     │    │
│  │  SQLite (webscan.db) + SQLite (toskill.db) + 文件系统 + 外部服务     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 前端架构设计

#### 旧版前端（front/）

旧版前端采用 **Vue 3 Composition API + Pinia 状态管理** 的现代SPA架构，使用 Element Plus 企业级组件库：

```
front/src/
├── App.vue                          # 根组件，挂载 AppLayout 全局布局
├── main.js                          # 入口文件: Vue/Pinia/Router/ElementPlus 注册
├── router/index.js                  # 路由定义 (15个路由 + 导航守卫 + 滚动恢复)
├── store/                           # Pinia 状态管理
│   ├── tasks.js                     # 任务状态 (localStorage持久化, ID规范化, 自动去重)
│   ├── settings.js                  # 设置状态 (4分类: general/scan/notification/security)
│   ├── vulnerabilities.js           # 漏洞状态 (多条件过滤, 严重度分布统计)
│   └── index.js                     # Pinia 实例导出
├── stores/
│   └── aiChat.js                    # AI对话WebSocket状态 (12种消息类型路由)
├── views/                           # 15个页面视图组件
│   ├── Dashboard.vue                # 仪表盘 (StatCard + Chart.js + 冻结任务)
│   ├── ScanTasks.vue                # 扫描任务管理 (WebSocket实时更新)
│   ├── AgentScan.vue                # AI Agent扫描 (双重WebSocket, 子图追踪)
│   ├── AWVSScan.vue                 # AWVS扫描 (6种扫描配置)
│   ├── POCScan.vue                  # POC扫描 (轮询+结果展示)
│   ├── VulnerabilityResults.vue     # 漏洞列表 (双标签: 结果+日志)
│   ├── VulnerabilityDetail.vue      # 漏洞详情 (CVSS评分+POC下载)
│   ├── Reports.vue                  # 报告管理 (HTML/PDF/JSON导出)
│   ├── ReportDetail.vue             # 报告详情 (AI分析区+打印支持)
│   ├── KnowledgeBase.vue            # 知识库 (本地+Seebug双搜索)
│   ├── Settings.vue                 # 系统设置 (4标签页)
│   ├── Notifications.vue            # 通知中心
│   ├── NotificationDetail.vue       # 通知详情
│   ├── Profile.vue                  # 用户资料 (9种权限+2FA)
│   └── NotFound.vue                 # 404页面
├── components/
│   ├── business/                    # 13个业务组件 (AIChatPanel, AgentScanForm, WorkflowTimeline等)
│   ├── common/                      # 6个通用组件 (Alert, Toast, Loading, StatCard等)
│   └── layout/
│       └── AppLayout.vue            # 全局布局 (侧边栏+通知+AI浮窗)
├── utils/                           # 16个工具模块
│   ├── api.js                       # Axios封装 + 9大API模块 (77+方法)
│   ├── websocket.js                 # WebSocketManager (24种消息类型+ACK)
│   ├── aiAgents.js                  # AI Agent API + ProgressWatcher
│   ├── workflowData.js              # 工作流数据标准化 (3核心类)
│   ├── useWorkflowRealtime.js       # 工作流实时Hook
│   ├── useLogWebSocket.js           # 日志WebSocket Hook
│   ├── validators.js                # 表单验证 (12种验证器+防抖节流)
│   ├── errorHandler.js              # 全局错误处理 (5种ErrorType)
│   └── helpers.js / date.js / icons.js / theme.js 等
└── styles/
    └── theme.js + CSS变量 + 响应式断点
```

旧版前端的关键设计决策：
1. **组件分层**：业务组件（business/）与通用组件（common/）分离，确保业务逻辑与UI展示解耦
2. **状态管理**：Pinia组合式Store + localStorage持久化，关键业务状态各自独立管理
3. **通信层**：4个独立WebSocket连接各司其职（通用任务更新、Agent进度、AI对话、日志查看），避免消息混乱
4. **错误处理**：4层错误捕获架构（Vue errorHandler → Promise rejection → Window error → 网络状态监听）

#### 新版前端（toskill-frontend/）

新版前端采用**极致轻量化**策略，无重型UI框架依赖，以WebSocket驱动的实时交互为核心：

```
toskill-frontend/src/
├── App.vue                          # 根组件 + 全局布局
├── main.js                          # 入口文件: Vue + Store + 全局组件注册
├── store.js                         # 单文件响应式Store (globalState + Toast + Modal)
├── components/
│   ├── AgentWorkspace/              # Agent交互工作空间 (核心)
│   │   ├── ChatArea.vue             # AI对话区域 (Markdown渲染)
│   │   ├── CommandInput.vue         # 命令输入组件
│   │   └── HistoryRail.vue          # 历史会话侧栏
│   ├── views/                       # 5个功能视图
│   │   ├── ConsoleView.vue          # 控制台 (AI对话主界面)
│   │   ├── ScanView.vue             # 扫描视图
│   │   ├── ToolsView.vue            # 工具列表
│   │   ├── ReportsView.vue          # 报告管理
│   │   └── SettingsView.vue         # 系统设置
│   ├── AppHeader.vue                # 顶部导航
│   ├── AppSidebar.vue               # 侧边栏导航
│   ├── FloatingLogPanel.vue         # 浮动日志面板
│   └── ScriptViewer.vue             # 脚本查看器
├── composables/                     # 组合式函数 (逻辑复用)
│   ├── useAgentChat.js              # Agent聊天WebSocket管理
│   ├── useLoadingState.js           # 加载状态管理
│   └── useLogBus.js                 # 日志事件总线
├── services/                        # 服务层
│   ├── api.js                       # REST API服务 (单例)
│   ├── websocket.js                 # WebSocket服务
│   └── storageService.js            # 本地存储服务
└── utils/
    └── errorHandler.js              # 错误处理
```

新版前端的关键设计决策：
1. **WebSocket驱动**：以WebSocket为主力通信方式，REST API仅作为辅助
2. **无路由设计**：单页视图切换，通过组件状态控制，无需Vue Router
3. **Agent工作空间**：ChatArea + CommandInput + HistoryRail 构成核心交互界面
4. **轻量化**：无Element Plus、无Pinia、无Chart.js，仅依赖Vue 3 + marked + Lucide Icons

### 1.4 后端架构设计

#### 旧版后端（backend/）

旧版后端采用 **FastAPI 异步框架 + 分层模块化** 架构，通过 `backend/api/__init__.py` 实现统一路由注册，同时挂载了TOSKill的API路由：

```
backend/
├── main.py                          # 应用入口 (生命周期管理/中间件/优雅关闭)
├── config.py                        # 配置管理 (Pydantic Settings + .env + 类型校验)
├── database.py                      # 数据库初始化 (Tortoise-ORM + 连接池 + 默认管理员)
├── models.py                        # 数据模型 (14张表 + 状态机 + 严格状态转换校验)
├── task_executor.py                 # 任务执行器 (~2500行, 核心调度引擎)
├── plugin_executor.py               # 插件多进程执行器 (进程隔离 + 心跳监控)
│
├── api/                             # API 路由层 (17个模块)
│   ├── __init__.py                  # 路由注册中心 (统一管理所有子路由, 含TOSKill)
│   ├── tasks.py                     # 任务 CRUD + 统计
│   ├── reports.py                   # 报告管理 + 多格式导出
│   ├── poc.py                       # POC 扫描
│   ├── awvs.py                      # AWVS 集成 (8个API类)
│   ├── ai.py                        # AI 对话
│   ├── kb.py                        # 漏洞知识库
│   ├── settings.py                  # 系统设置
│   ├── notifications.py             # 通知管理
│   ├── seebug.py                    # Seebug 集成
│   ├── user.py                      # 用户管理
│   ├── websocket.py                 # WebSocket 端点
│   ├── logging_middleware.py        # API 日志中间件
│   └── task_type_registry.py        # 任务类型注册表
│
├── ai_agents/                       # AI Agent 系统
│   ├── core/graph.py                # LangGraph 图构建 (4个子图)
│   ├── core/nodes.py                # 节点定义 (模板方法模式)
│   ├── core/state.py                # AgentState 状态
│   ├── tools/registry.py            # 工具注册表
│   ├── tools/adapters.py            # 工具适配器
│   ├── analyzers/                   # 分析器 (AI分析/漏洞分析/报告生成)
│   ├── poc_system/                  # POC 验证系统 (动态引擎/匹配/评分)
│   └── api/routes.py                # Agent API 路由
│
├── plugins/                         # 信息收集插件 (14个)
├── vulnerability_scan_plugins/      # 漏洞扫描插件 (9个 + Payload库)
├── poc/                             # POC 验证脚本 (14个, 7大框架)
├── AVWS/                            # AWVS REST API 封装 (8个API类)
├── services/                        # 服务层 (通知/报告)
└── utils/                           # 工具层 (日志/序列化/Seebug)
```

#### 新版后端（TOSKill/）

新版后端同样采用 **FastAPI**，但以 **LangGraph工作流引擎** 为核心，强调AI驱动的实时交互：

```
TOSKill/
├── main.py                          # 独立应用入口 (监听8081, 启动时AI模型检测)
├── config.py                        # 独立配置 (TOSKillSettings, 8081端口)
│
├── AI/                              # AI核心模块
│   ├── graph.py                     # LangGraph工作流 (3子图: IntentRecognition + InfoCollection + VulnScan + Report)
│   ├── state.py                     # ScanState (TypedDict, 会话状态+认证信息+执行历史)
│   ├── tools.py                     # 22个@tool装饰器注册 (统一ToolResult格式)
│   ├── core.py                      # 核心业务逻辑 (意图识别, 工具调度)
│   ├── llm_client.py                # LLM客户端 (可用性检测, 回退策略)
│   ├── validators.py                # 输入验证器
│   ├── script_safety.py             # 脚本安全审查 (AST分析+恶意模式)
│   ├── model_check.py               # AI模型连接验证
│   └── log_collector.py             # 日志收集器
│
├── RAG/                             # RAG知识库模块
│   ├── rag_engine.py                # RAG引擎 (LlamaIndex, HuggingFace嵌入)
│   ├── retriever.py                 # 检索器 (工具-知识映射, 上下文扩充)
│   ├── knowledge/                   # 14个专业知识文档
│   └── storage/                     # 向量存储
│
├── api/                             # API接口层
│   ├── ai_chat_websocket.py         # WebSocket处理 (会话管理, 双向消息路由)
│   ├── scan_api.py                  # 扫描接口 (单工具/批量/全流程)
│   ├── log_websocket.py             # 实时日志推送
│   └── report.py                    # 报告生成与下载
│
├── tools/                           # 安全工具
│   ├── info_collection/             # 11个信息收集工具
│   ├── vuln_scan/                   # 8个漏洞扫描工具
│   ├── poc/                         # 3个POC验证工具
│   └── report/                      # 4个报告生成工具
│
├── analysis/                        # 分析模块
│   └── result_analyzer.py           # 结果分析器
│
└── utils/                           # 工具模块
    ├── error_handler.py             # 错误处理
    └── log_writer.py                # 日志写入
```

### 1.5 通信架构设计

系统采用 **HTTP REST + WebSocket 双通道** 通信架构，其中新版系统以WebSocket为主要通信方式：

```
前端                                        后端
┌──────────────────┐   HTTP REST API      ┌──────────────────┐
│                  │ ───────────────────→ │                  │
│  Axios / Fetch   │   POST /api/tasks    │  FastAPI Router  │
│  (9大API模块)     │   GET  /api/reports  │  (17+6个子路由)   │
│  (统一拦截器)     │   PUT  /api/settings │  (Pydantic校验)  │
│                  │ ←─────────────────── │                  │
└──────────────────┘   JSON Response      └──────────────────┘
        │                                          │
        │          WebSocket (多连接)                │
        │ ┌──────────────────────────────────────┐ │
        ├─┤ 旧版WS #1: 通用任务更新 (24种消息类型)  ├─┤
        ├─┤ 旧版WS #2: Agent专用进度 (7种消息类型)  ├─┤
        ├─┤ 旧版WS #3: AI对话 (12种消息类型)       ├─┤
        ├─┤ 旧版WS #4: 日志查看 (6种消息类型)       ├─┤
        │ │                                      │ │
        │ │ 新版WS: AI对话+扫描 (16种消息类型)      │ │
        │ │ (TOSKill ai_chat_websocket)           │ │
        │ └──────────────────────────────────────┘ │
                                                   │
                                          ┌────────┴────────┐
                                          │ ConnectionManager│
                                          │ (连接状态追踪)    │
                                          │ (广播+自动清理)   │
                                          │ (优雅关闭)       │
                                          └─────────────────┘
```

**WebSocket 消息类型汇总：**

| 方向 | 消息类型 | 说明 | 系统 |
|------|---------|------|------|
| S→C | task_update | 任务状态变更 | 旧版 |
| S→C | task_progress | 任务进度更新 | 旧版 |
| S→C | task_completed | 任务执行完成 | 旧版 |
| S→C | task_failed | 任务执行失败 | 旧版 |
| S→C | stage_update | Agent阶段更新 | 旧版 |
| S→C | subgraph_progress | 子图进度 | 旧版 |
| S→C | tool_execution | 工具执行事件 | 旧版 |
| S→C | vulnerability_found | 发现漏洞 | 旧版 |
| S→C | notification | 系统通知 | 旧版 |
| S→C | ai_message | AI消息 | 双系统 |
| S→C | decision | AI决策 | 旧版 |
| S→C | connected | 连接成功(session_id) | 新版 |
| S→C | scan_started | 扫描已启动 | 新版 |
| S→C | task_started | 单个任务开始 | 新版 |
| S→C | task_completed | 单个任务完成 | 新版 |
| S→C | task_skipped | 任务跳过 | 新版 |
| S→C | ai_decision | AI决策结果 | 新版 |
| S→C | workflow_progress | 工作流进度 | 新版 |
| S→C | interaction_required | 需要用户交互 | 新版 |
| S→C | workflow_resumed | 工作流已恢复 | 新版 |
| S→C | high_risk_vulnerability_detected | 高危漏洞检测 | 新版 |
| S→C | scan_completed | 扫描完成 | 新版 |
| S→C | report_generated | 报告已生成 | 新版 |
| S→C | error | 错误消息 | 新版 |
| C→S | ping | 心跳检测 | 双系统 |
| C→S | chat_message | AI对话 | 旧版 |
| C→S | start_scan | 启动扫描 | 新版 |
| C→S | user_confirm | 用户确认选择 | 新版 |
| C→S | chat | AI对话消息 | 新版 |
| C→S | message_ack | 消息确认 | 旧版 |

### 1.6 本章小结

本章从设计原则、总体架构、前后端架构和通信架构四个维度阐述了系统的整体设计。系统采用前后端分离的B/S架构，支持双系统并行部署。旧版系统以成熟的Vue 3 + Element Plus + FastAPI技术栈提供完整的Web扫描功能，新版系统以轻量化的Vue 3 + LangGraph + WebSocket技术栈探索AI驱动的智能扫描模式。两套系统通过后端路由统一注册实现无缝集成，在通信层共用HTTP REST + WebSocket双通道架构。

---

## 2. 功能模块设计

### 2.1 模块总体划分

系统按功能划分为 **8大核心模块**，各模块职责明确、接口清晰，在双系统中各有侧重：

```
┌─────────────────────────────────────────────────────────────────────┐
│                      WebScan AI 安全平台                              │
│                                                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ 信息收集  │  │ 漏洞扫描  │  │ POC验证  │  │ 报告生成  │           │
│  │ (14个插件)│  │ (9个扫描器)│  │ (14个POC) │  │ (多格式)  │           │
│  │ 旧版主力  │  │ 旧版主力  │  │ 双系统   │  │ 双系统   │           │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘           │
│        │             │             │             │                   │
│        └─────────────┼─────────────┼─────────────┘                   │
│                      │             │                                  │
│              ┌───────┴─────────────┴───────┐                         │
│              │      AI Agent 工作流引擎     │                         │
│              │   (LangGraph 多子图协同)     │                         │
│              │   旧版4子图 | 新版3子图       │                         │
│              └─────────────────────────────┘                         │
│                      │             │                                  │
│        ┌─────────────┼─────────────┼─────────────┐                   │
│        │             │             │             │                   │
│  ┌─────┴────┐  ┌─────┴────┐  ┌─────┴────┐  ┌─────┴────┐           │
│  │ 脚本管理  │  │ AWVS集成 │  │ 知识库   │  │ 通知系统  │           │
│  │ (上传+生成)│  │ (8个API) │  │ (双搜索) │  │ (WebSocket)│           │
│  │ 双系统   │  │ 旧版独有  │  │ 旧版+新版 │  │ 旧版独有  │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 信息收集模块

信息收集模块负责在漏洞扫描前获取目标系统的全面信息，旧版系统包含 **14个信息收集插件**，新版系统包含 **11个信息收集工具**：

| 工具/插件名称 | 功能描述 | 旧版 | 新版 | 超时(秒) |
|-------------|---------|------|------|---------|
| baseinfo | 获取HTTP头、服务器信息、SSL证书等基础信息 | ✓ | ✓ | 60 |
| portscan | 检测目标主机的开放端口和运行服务 | ✓ | ✓ | 120 |
| subdomain | 发现目标域名的子域名资产 | ✓ | ✓ | 120 |
| dirscan | 发现目标网站的隐藏目录和文件 | ✓ | ✓ | 180 |
| crawler | Web爬虫，提取链接、表单和参数 | ✓ | - | 300 |
| waf | 识别目标是否部署Web应用防火墙 | ✓ | ✓ | 60 |
| cdnexist | 判断目标是否使用CDN加速 | ✓ | ✓ | 30 |
| whatcms | 识别目标网站的内容管理系统 | ✓ | ✓ | 60 |
| infoleak | 检测敏感信息泄露风险 | ✓ | ✓ | 60 |
| webside | 查询网站备案等基本信息 | ✓ | ✓ | 60 |
| webweight | 查询网站的搜索引擎权重 | ✓ | ✓ | 30 |
| iplocating | 查询目标IP的地理位置信息 | ✓ | ✓ | 30 |
| loginfo | 获取日志相关信息 | ✓ | ✓ | 30 |
| randheader | 生成随机HTTP请求头 | ✓ | ✓ | 30 |

> 注：新版系统未包含crawler爬虫工具，但新增了TOSKill独有的RAG知识增强能力。

### 2.3 漏洞扫描模块

漏洞扫描模块覆盖 **OWASP Top 10** 等主流Web安全漏洞类型，旧版包含 **9个漏洞扫描器**，新版包含 **8个漏洞扫描工具**：

| 扫描器 | 检测漏洞 | 扫描方法 | 技术特点 |
|--------|---------|---------|---------|
| SQLiScanner | SQL注入 | 报错注入 + 时间盲注 + 布尔盲注 + Union注入 | 多维度综合判定，数据库类型识别 |
| XSSScanner | 跨站脚本 | 反射型 + 存储型 + DOM型 | HTML上下文感知注入 |
| CSRFScanner | 跨站请求伪造 | 令牌缺失检测 | Token + Referer双重验证 |
| SSRFScanner | 服务端请求伪造 | 内网地址探测 | 协议利用 + 云元数据检测 |
| LFIScanner | 文件包含 | 路径穿越 + 日志注入 | 多层目录遍历 |
| CmdiScanner | 命令注入 | OS命令注入 | 多平台Payload适配 |
| FileUploadScanner | 文件上传 | 扩展名绕过 + 内容检测 | 双扩展名 + 图片马检测 |
| WeakPassScanner | 弱口令 | 字典爆破 | 多服务类型支持 |
| InfoLeakScanner | 信息泄露 | 敏感信息检测 | 模式匹配 + 上下文分析（仅旧版） |

漏洞扫描器采用**模板方法模式**设计，基类定义统一扫描流程：

```
┌─────────────────────────────────┐
│      BaseScanner (抽象基类)       │
│                                 │
│  + scan(target) → ScanResult   │  ← 扫描入口 (模板方法)
│  + build_payloads() → List     │  ← 从Payload库加载
│  + analyze_response() → Finding│  ← 响应分析判定
│  + _create_vulnerability()     │  ← 创建标准化漏洞对象
│  + record_request_response()   │  ← 审计记录
└─────────────────────────────────┘
        ↑           ↑           ↑
   ┌────┴───┐  ┌────┴───┐  ┌────┴───┐
   │SQLiScanner│ │XSSScanner│ │CmdiScanner│ ...
   └──────────┘  └──────────┘  └──────────┘
```

以SQL注入扫描器为例，其检测流程为：
1. **发现注入点**：解析URL参数 + 提取表单字段 + 检测HTTP头
2. **建立基准**：发送正常请求，记录基准响应特征
3. **错误回显注入**：使用数据库特定Payload，匹配错误模式
4. **时间盲注**：基准时间 vs 延时响应对比，80%阈值判定
5. **布尔盲注**：True/False Payload对比（响应长度差异>100字节 + 相似度阈值）
6. **Union注入**：ORDER BY探测列数 + UNION SELECT验证

### 2.4 POC验证模块

POC验证模块针对已知框架漏洞提供精确验证能力，包含 **14个POC脚本**（旧版），覆盖7大主流框架：

| 框架 | CVE/ID | 漏洞类型 | 验证方式 |
|------|--------|---------|---------|
| **WebLogic** | CVE-2020-2551 | IIOP反序列化 | T3协议探测 |
| | CVE-2018-2628 | T3反序列化 | T3协议探测 |
| | CVE-2018-2894 | 任意文件上传 | 上传验证 |
| | CVE-2020-14756 | IIOP反序列化 | T3协议探测 |
| | CVE-2023-21839 | 远程代码执行 | RCE验证 |
| **Struts2** | S2-009 | OGNL执行 | OGNL注入 |
| | S2-032 | DMI执行 | DMI利用 |
| **Tomcat** | CVE-2017-12615 | PUT任意文件上传 | PUT方法验证 |
| | CVE-2022-22965 | Spring4Shell | 属性绑定 |
| | CVE-2022-47986 | IBM Aspera RCE | RCE验证 |
| **JBoss** | CVE-2017-12149 | 反序列化 | 序列化探测 |
| **Nexus** | CVE-2020-10199 | OGNL注入 | OGNL验证 |
| **Drupal** | CVE-2018-7600 | 远程代码执行 | RCE验证 |
| **ThinkPHP** | TP-99617 | 远程代码执行 | AI生成POC |

POC验证引擎（VerificationEngine）具备完整的企业级特性：

```
┌──────────────────────────────────────────────────────────┐
│                   VerificationEngine                      │
│                                                          │
│  ┌────────────────┐  ┌────────────────┐                 │
│  │ 优先级队列      │  │ 资源监控器      │                 │
│  │ (heapq实现)    │  │ (psutil)       │                 │
│  │ LOW/NORMAL/    │  │ 内存+CPU监控   │                 │
│  │ HIGH/CRITICAL  │  │ 自动节流/暂停   │                 │
│  └───────┬────────┘  └────────────────┘                 │
│          │                                               │
│  ┌───────┴────────┐  ┌────────────────┐                 │
│  │ 并发信号量      │  │ 结果缓存        │                 │
│  │ (Semaphore)    │  │ (SHA256键+TTL) │                 │
│  │ 动态调整并发数  │  │ 避免重复执行    │                 │
│  └───────┬────────┘  └────────────────┘                 │
│          │                                               │
│  ┌───────┴────────────────────────────────────────┐     │
│  │              执行流程                            │     │
│  │  获取POC代码 → 检查缓存 → 执行(含指数退避重试)    │     │
│  │  → 计算置信度/严重度/CVSS → 保存结果 → 记录日志   │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

**置信度评分系统**采用3维权重模型：
- **匹配度权重（30%）**：POC与目标框架/CMS的匹配程度
- **执行度权重（40%）**：POC实际执行后的响应特征吻合度
- **输出度权重（30%）**：POC输出结果与预期漏洞特征的匹配度

### 2.5 AI Agent工作流模块

AI Agent工作流是平台的核心智能引擎，基于 **LangGraph** 框架构建。两套系统的工作流有所不同：

**旧版系统（4子图）**：

```
┌─────────────────────────────────────────────────────────────────────┐
│              旧版 AI Agent 工作流 (4子图顺序执行)                      │
│                                                                       │
│  ┌───────────────┐    ┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│  │ 信息收集子图   │───→│ 漏洞扫描子图   │───→│ POC验证子图    │───→│ 结果分析子图   │
│  │ recursion=100 │    │ recursion=200 │    │ recursion=200 │    │ recursion=100 │
│  │               │    │               │    │               │    │               │
│  │ 环境感知节点   │    │ 扫描规划节点   │    │ POC规划节点    │    │ 漏洞分析节点   │
│  │ 任务规划节点   │    │ 工具执行节点   │    │ POC执行节点    │    │ 报告生成节点   │
│  │ 工具执行节点   │    │ 结果汇总节点   │    │ 结果验证节点   │    │ (AI增强)     │
│  │ 结果验证节点   │    │               │    │               │    │               │
│  └───────────────┘    └───────────────┘    └───────────────┘    └───────────────┘
│                                                                       │
│  状态传递: AgentState ──→ AgentState ──→ AgentState ──→ AgentState    │
│            (to_dict/from_dict 序列化，无缝传递)                        │
└─────────────────────────────────────────────────────────────────────┘
```

**新版系统（3子图 + 意图识别）**：

```
┌─────────────────────────────────────────────────────────────────────┐
│              新版 TOSKill AI Agent 工作流                             │
│                                                                       │
│  ┌───────────────────────┐                                           │
│  │  IntentRecognitionGraph│  ← 意图识别: 聊天/扫描/工具执行            │
│  │  用户意图识别入口       │                                           │
│  │  意图校验 → 工具存在性检查│                                          │
│  └───────────┬───────────┘                                           │
│              │                                                       │
│     ┌────────┼────────┐                                              │
│     ▼        ▼        ▼                                              │
│  ┌────────┐┌────────┐┌────────┐                                     │
│  │ 聊天模式││工具执行 ││ 扫描模式│                                     │
│  └────────┘└────────┘└───┬────┘                                     │
│                          │                                           │
│              ┌───────────┼───────────┐                               │
│              ▼           ▼           ▼                               │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐              │
│  │InfoCollection │ │  VulnScan    │ │   Report      │              │
│  │信息收集子图    │ │  漏洞扫描子图  │ │  报告生成子图  │              │
│  └───────────────┘ └───────────────┘ └───────────────┘              │
│                                                                       │
│  ReACT推理循环:                                                       │
│  ai_decision → user_interact → router → execute_task → vuln_check    │
│       │            │            │          │             │            │
│       ▼            ▼            ▼          ▼             ▼            │
│    AI决策      用户交互      路由分发   执行工具       漏洞检查         │
│    (RAG增强)   (interrupt)            (认证共享)     (高危中断)        │
└─────────────────────────────────────────────────────────────────────┘
```

**AgentState 核心状态设计（旧版）：**

```python
@dataclass
class AgentState:
    # 基础信息
    target: str                           # 扫描目标
    task_id: str                          # 任务 ID

    # 任务规划
    planned_tasks: List[str]              # 规划的任务列表
    current_task: Optional[str]           # 当前执行任务
    completed_tasks: List[str]            # 已完成任务列表

    # 执行结果
    tool_results: Dict[str, Any]          # 工具执行结果集合
    vulnerabilities: List[Dict]           # 发现的漏洞列表
    target_context: Dict[str, Any]        # 目标上下文 (CMS/端口/WAF/CDN等)

    # 记忆与上下文
    execution_history: List[Dict]         # 执行历史记录
    user_requirement: str                 # 用户需求描述
    seebug_pocs: List[Dict]               # Seebug POC 列表
    generated_pocs: List[Dict]            # 生成的 POC 列表

    # 控制开关
    is_complete: bool                     # 是否完成
    should_continue: bool                 # 是否继续执行

    # 阶段追踪
    stage_status: Dict[str, Dict]         # 4个阶段的进度/状态
    workflow_trace: Optional[WorkflowTrace]  # 工作流追踪记录

    # 扫描摘要与报告
    scan_summary: Dict[str, Any]          # 扫描摘要
    report: str                           # 最终报告
```

**新版系统关键差异**：TOSKill使用 TypedDict 而非 dataclass 定义状态，增加了 `auth_info`（认证信息）、`session_id`（会话ID）、`interrupt_data`（中断数据）等字段，支持 interrupt 用户交互机制。

### 2.6 报告生成模块

报告生成模块负责将扫描结果转化为结构化的安全评估报告，支持 **HTML、PDF、JSON** 三种格式导出。增强报告生成器（旧版）集成了AI分析能力：

```
┌──────────────────────────────────────────────┐
│           EnhancedReportGenerator             │
│                                              │
│  输入: AgentState + TaskName                  │
│                                              │
│  ┌──────────────────────────────────────┐   │
│  │ 步骤1: 数据收集                       │   │
│  │  • 漏洞列表 (按严重度排序)             │   │
│  │  • 工具执行结果汇总                   │   │
│  │  • 执行历史记录                       │   │
│  └──────────────┬───────────────────────┘   │
│                 ↓                             │
│  ┌──────────────────────────────────────┐   │
│  │ 步骤2: AI 分析增强                    │   │
│  │  • 漏洞成因分析                       │   │
│  │  • 利用风险评估                       │   │
│  │  • 修复优先级排序                     │   │
│  │  • 业务影响评估                       │   │
│  └──────────────┬───────────────────────┘   │
│                 ↓                             │
│  ┌──────────────────────────────────────┐   │
│  │ 步骤3: 报告生成                       │   │
│  │  • JSON 结构化报告                    │   │
│  │  • HTML 富文本报告 (含图表)           │   │
│  │  • PDF 格式报告                       │   │
│  └──────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
```

新版系统（TOSKill）的报告模块支持 Markdown 格式报告，通过 `TOSKill/tools/report/` 下的 html_report_generator.py、report_manager.py、vuln_analyzer.py、ai_analyzer.py 四个模块实现报告生成流程。

### 2.7 脚本管理模块

脚本管理模块支持用户上传自定义扫描脚本和AI自动生成脚本，所有脚本均需通过**多阶段安全审查**。该模块在双系统中均有实现：

```
┌──────────────────────────────────────────────────────┐
│                 脚本安全审查流程                        │
│                                                      │
│  用户上传脚本 / AI生成脚本                              │
│         │                                            │
│         ↓                                            │
│  ┌─────────────────────────────────────────────┐    │
│  │ 第1阶段: 静态分析 (AST解析)                   │    │
│  │  • 检测危险函数: eval, exec, os.system       │    │
│  │  • 检测文件操作: open, os.remove             │    │
│  │  • 检测网络操作: socket, requests            │    │
│  └────────────────────┬────────────────────────┘    │
│                       ↓                              │
│  ┌─────────────────────────────────────────────┐    │
│  │ 第2阶段: 结构验证                              │    │
│  │  • 检查必需函数: run(target)                  │    │
│  │  • 验证返回类型: Dict                         │    │
│  └────────────────────┬────────────────────────┘    │
│                       ↓                              │
│  ┌─────────────────────────────────────────────┐    │
│  │ 第3阶段: 内容审查                              │    │
│  │  • 检测恶意模式: 反弹Shell, 下载执行, 提权    │    │
│  └────────────────────┬────────────────────────┘    │
│                       ↓                              │
│  ┌─────────────────────────────────────────────┐    │
│  │ 第4阶段: AI辅助分析                            │    │
│  │  • 分析脚本功能描述                            │    │
│  │  • 生成工具名称和分类                          │    │
│  └────────────────────┬────────────────────────┘    │
│                       ↓                              │
│              注册为可用工具                            │
└──────────────────────────────────────────────────────┘
```

### 2.8 前端页面模块

#### 旧版前端页面（15个页面视图）

| 路由路径 | 页面组件 | 功能描述 |
|---------|---------|---------|
| `/` | Dashboard | 仪表盘首页，展示扫描统计、任务概览、趋势图 |
| `/scan-tasks` | ScanTasks | 扫描任务管理，创建/查看/管理各类扫描任务 |
| `/poc-scan` | POCScan | POC扫描页面，选择POC类型执行框架漏洞验证 |
| `/agent-scan` | AgentScan | AI Agent扫描，使用AI Agent执行智能扫描 |
| `/awvs-scan` | AWVSScan | AWVS集成扫描，对接Acunetix扫描器 |
| `/vulnerability-results` | VulnerabilityResults | 漏洞结果列表，双标签（结果+日志） |
| `/vulnerability/:id` | VulnerabilityDetail | 漏洞详情，CVSS评分+POC下载 |
| `/reports` | Reports | 报告列表，多格式导出（HTML/PDF/JSON） |
| `/report-detail` | ReportDetail | 报告详情，AI分析独立区域+打印支持 |
| `/knowledge-base` | KnowledgeBase | 漏洞知识库，本地+Seebug双搜索 |
| `/settings` | Settings | 系统设置，4标签页（通用/扫描/通知/安全） |
| `/notifications` | Notifications | 通知中心，状态/类型筛选+批量已读 |
| `/notification/:id` | NotificationDetail | 通知详情页面 |
| `/profile` | Profile | 用户资料，9种权限+2FA |
| `/:pathMatch(.*)*` | NotFound | 404页面 |

#### 新版前端页面（5个功能视图）

| 视图组件 | 功能描述 |
|---------|---------|
| ConsoleView | AI对话式扫描主界面，集成Agent工作空间（ChatArea + CommandInput + HistoryRail） |
| ScanView | 扫描视图，支持目标输入和扫描模式选择 |
| ToolsView | 工具列表展示，按分类浏览可用安全工具 |
| ReportsView | 报告管理，查看和下载扫描报告 |
| SettingsView | 系统设置，配置AI模型和扫描参数 |

### 2.9 本章小结

本章详细阐述了系统的8大核心功能模块设计。信息收集、漏洞扫描、POC验证三大检测模块构成了系统的核心检测能力，AI Agent工作流引擎作为智能中枢驱动整个扫描流程，报告生成和脚本管理模块提供结果输出和扩展能力。两套系统在功能模块上各有侧重：旧版系统覆盖完整的传统扫描流程和外部集成（AWVS、Seebug），新版系统专注于AI驱动的智能交互式扫描。

---

## 3. 数据库设计

### 3.1 实体关系设计

旧版系统共定义 **14张数据表**，核心实体关系如下：

```
┌──────────┐     1:N     ┌──────────┐     1:N     ┌──────────────┐
│   Task   │────────────→│  Report  │────────────→│ AI Analysis  │
│ (状态机)  │────────────→│Vulnerability│          │ (AI增强分析) │
│          │────────────→│ScanResult │            └──────────────┘
│          │────────────→│POCResult  │
└──────────┘             └──────────┘

┌──────────────┐   1:N   ┌───────────────┐
│AIChatInstance│────────→│ AIChatMessage  │
└──────────────┘         └───────────────┘

┌──────────┐     1:N     ┌──────────────┐
│   User   │────────────→│ Notification │
└──────────┘             └──────────────┘

┌──────────────┐   1:N   ┌────────────────────┐
│ AgentTask    │────────→│ AgentResult         │
└──────────────┘         └────────────────────┘

┌──────────────────┐  1:N  ┌───────────────────────┐
│WorkflowExecution │──────→│WorkflowNodeExecution   │
│  (UUID主键)       │──────→│WorkflowTaskPlan        │
└──────────────────┘       └───────────────────────┘

┌──────────────────┐  1:N  ┌───────────────────────┐
│POCVerificationTask│─────→│POCVerificationResult  │
│  (优先级+资源控制) │      └──────┬────────────────┘
└──────────────────┘              │ 1:N
                                  ↓
                           ┌──────────────┐
                           │POCExecutionLog│
                           └──────────────┘

独立表: SystemSettings, SystemLog, VulnerabilityKB
```

> 新版系统（TOSKill）采用内存状态管理 + 可选SQLite持久化，不依赖ORM，状态数据通过LangGraph的MemorySaver和会话存储（TTL过期清理）管理。

### 3.2 核心数据表结构

**Task（扫描任务表）** — 核心业务表，实现严格状态机：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT (PK) | 任务ID，自增主键 |
| task_name | VARCHAR | 任务名称 |
| task_type | VARCHAR | 任务类型（scan/poc/awvs/ai_agent等） |
| target | VARCHAR | 扫描目标URL/IP |
| status | VARCHAR | 任务状态（状态机控制） |
| progress | INT | 执行进度（0-100） |
| config | JSON | 扫描配置（JSON格式） |
| result | JSON | 执行结果（JSON格式） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

**状态机转换规则：**

```python
valid_transitions = {
    "pending":    ["running", "cancelled", "failed", "processing"],
    "running":    ["completed", "failed", "cancelled", "processing", "pending"],
    "processing": ["running", "completed", "failed", "cancelled", "pending"],
    "completed":  ["pending", "running", "processing"],
    "failed":     ["pending", "running", "processing"],
    "cancelled":  ["pending", "running", "processing"]
}
```

**Vulnerability（漏洞信息表）：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT (PK) | 漏洞ID |
| task_id | INT (FK) | 关联任务ID |
| vuln_type | VARCHAR | 漏洞类型（sqli/xss/csrf等） |
| severity | VARCHAR | 严重程度（critical/high/medium/low） |
| title | VARCHAR | 漏洞标题 |
| url | VARCHAR | 漏洞URL |
| payload | TEXT | 攻击载荷 |
| evidence | TEXT | 漏洞证据 |
| ai_analysis | TEXT | AI分析结果 |
| risk_score | FLOAT | 风险评分 |
| cvss_score | FLOAT | CVSS评分 |

**WorkflowExecution（工作流执行表）：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | UUID (PK) | 工作流执行ID |
| task_id | INT | 关联任务ID |
| workflow_name | VARCHAR | 工作流名称 |
| target | VARCHAR | 扫描目标 |
| status | VARCHAR | 执行状态 |
| progress | INT | 执行进度 |
| graph_flow | JSON | 工作流图结构 |
| vulnerabilities | JSON | 发现的漏洞列表 |

**VulnerabilityKB（漏洞知识库表）：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT (PK) | 知识库条目ID |
| cve_id | VARCHAR (UNIQUE) | CVE编号 |
| name | VARCHAR | 漏洞名称 |
| severity | VARCHAR | 严重程度 |
| cvss_score | FLOAT | CVSS评分 |
| poc_code | TEXT | POC代码 |
| remediation | TEXT | 修复建议 |

### 3.3 本章小结

本章介绍了系统的数据库设计，重点阐述了旧版系统的14张核心数据表及其ER关系。旧版系统采用SQLite + Tortoise-ORM实现数据持久化，其中Task表通过严格状态机保证任务状态转换的正确性。新版系统采用轻量级的内存状态管理，依赖LangGraph的MemorySaver实现会话状态的TTL过期清理，体现了双系统在数据持久化策略上的差异化设计。

---

## 4. 接口设计

### 4.1 REST API 设计

所有API统一前缀 `/api`，遵循RESTful规范，使用JSON格式进行数据交换。旧版后端通过 `backend/api/__init__.py` 统一注册旧版和新版（TOSKill）的所有路由。

**API端点总览：**

| 模块 | 前缀 | 方法数 | 所属系统 | 说明 |
|------|------|--------|---------|------|
| 任务管理 | /api/tasks | 11 | 旧版 | 任务CRUD、取消、统计、冻结 |
| 报告管理 | /api/reports | 11 | 旧版 | 报告查询、导出、AI重新分析 |
| POC扫描 | /api/poc | 4 | 旧版 | POC类型查询、执行扫描、下载 |
| AWVS扫描 | /api/awvs | 14 | 旧版 | 目标/扫描/漏洞管理、健康检查 |
| AI对话 | /api/ai | - | 旧版 | 对话实例、消息管理 |
| 知识库 | /api/kb | 7 | 旧版 | 漏洞查询、Seebug同步、POC下载 |
| 系统设置 | /api/settings | 14 | 旧版 | 配置CRUD、系统信息、统计 |
| 通知管理 | /api/notifications | 8 | 旧版 | 通知CRUD、已读标记、未读计数 |
| Seebug | /api/seebug | 4 | 旧版 | 搜索、POC详情、连接测试 |
| 用户管理 | /api/user | 4 | 旧版 | 用户信息、权限 |
| AI Agents | /api/agents | 20+ | 旧版 | Agent任务管理、工具/配置/代码生成 |
| WebSocket | /ws, /ws/progress | - | 旧版 | 实时通信 |
| TOSKill扫描 | /api/scan | 6 | 新版 | 信息收集/漏洞扫描/完整扫描 |
| TOSKill工具 | /api/tools | 4 | 新版 | 工具列表/分类/执行/批量执行 |
| TOSKill报告 | /api/reports | 4 | 新版 | 报告列表/下载/内容查看/删除 |
| TOSKill对话 | /api/chat | 2 | 新版 | 发送消息/历史记录 |
| TOSKill会话 | /api/sessions | 3 | 新版 | 创建/状态/删除会话 |
| TOSKill脚本 | /api/scripts | - | 新版 | 脚本上传/管理 |
| TOSKill WebSocket | /api/ai-chat/ws | - | 新版 | AI对话WebSocket |
| TOSKill日志WS | /api/logs/ws | - | 新版 | 实时日志WebSocket |

**核心API接口示例：**

**（1）创建扫描任务**

```
POST /api/tasks/create
Content-Type: application/json

请求体:
{
  "task_name": "安全扫描 - example.com",
  "task_type": "ai_agent_scan",
  "target": "https://example.com",
  "scan_config": {
    "strategy": "standard",
    "concurrency": 5,
    "timeout": 300,
    "selected_tools": ["baseinfo", "portscan", "sqli_scan"],
    "user_requirement": "重点关注SQL注入漏洞",
    "poc_types": ["weblogic_cve_2020_2551"]
  }
}

响应:
{
  "code": 200,
  "message": "任务创建成功",
  "data": {
    "task_id": 42,
    "status": "pending",
    "progress": 0
  }
}
```

**（2）TOSKill完整扫描（新版）**

```
POST /api/scan/full
Content-Type: application/json

请求体:
{
  "target": "example.com"
}
```

**（3）TOSKill工具执行（新版）**

```
POST /api/tools/execute
Content-Type: application/json

请求体:
{
  "tool_name": "sqli",
  "target": "http://example.com/page?id=1"
}
```

### 4.2 WebSocket 消息协议设计

WebSocket通信采用JSON格式的消息协议，消息结构统一为：

```json
{
  "type": "消息类型",
  "payload": {
    // 消息载荷
  },
  "timestamp": "ISO8601时间戳"
}
```

**新版系统（TOSKill）WebSocket消息类型**（`/api/ai-chat/ws`）：

| 方向 | 消息类型 | 说明 |
|------|---------|------|
| S→C | connected | 连接成功，返回session_id |
| S→C | scan_started | 扫描已启动 |
| S→C | task_started | 单个任务开始 |
| S→C | task_completed | 单个任务完成 |
| S→C | task_skipped | 任务跳过 |
| S→C | ai_decision | AI决策结果 |
| S→C | workflow_progress | 工作流进度 |
| S→C | interaction_required | 需要用户交互（interrupt） |
| S→C | workflow_resumed | 工作流已恢复 |
| S→C | high_risk_vulnerability_detected | 高危漏洞检测 |
| S→C | scan_completed | 扫描完成 |
| S→C | report_generated | 报告已生成 |
| S→C | ai_message | AI对话消息 |
| S→C | error | 错误消息 |
| C→S | start_scan | 启动扫描 |
| C→S | user_confirm | 用户确认选择（1=执行/2=停止/3=聊天） |
| C→S | chat | AI对话消息 |

### 4.3 本章小结

本章阐述了系统的接口设计。REST API方面，旧版后端通过路由注册中心统一管理17个旧版模块和6个TOSKill模块的所有端点。WebSocket方面，旧版系统维护4个独立连接（通用任务更新、Agent进度、AI对话、日志查看），新版系统以单个WebSocket连接驱动全流程（AI对话+扫描+日志），体现了从"多连接分工"到"单连接全双工"的架构演进。

---

## 5. 核心功能实现

### 5.1 AI Agent工作流引擎实现

AI Agent工作流引擎是平台最核心的智能模块，基于LangGraph实现多子图协同工作。两套系统在实现上有所不同。

**（1）旧版系统：4子图顺序执行**

```python
def _build_info_collection_graph(self) -> StateGraph:
    """构建信息收集子图"""
    workflow = StateGraph(AgentState)

    # 注册4个节点
    workflow.add_node("environment_awareness", env_node)    # 环境感知
    workflow.add_node("info_task_planning", planning_node)  # 任务规划
    workflow.add_node("info_tool_execution", execution_node) # 工具执行
    workflow.add_node("info_result_verification", verification_node) # 结果验证

    # 线性边：环境感知 → 任务规划 → 工具执行 → 结果验证
    workflow.set_entry_point("environment_awareness")
    workflow.add_edge("environment_awareness", "info_task_planning")
    workflow.add_edge("info_task_planning", "info_tool_execution")
    workflow.add_edge("info_tool_execution", "info_result_verification")

    # 条件边：验证后决定继续执行还是完成
    workflow.add_conditional_edges(
        "info_result_verification",
        self._should_continue_info_collection,
        {"continue": "info_tool_execution", "complete": END}
    )

    return workflow.compile(recursion_limit=100)
```

4个子图按顺序执行，状态通过序列化/反序列化无缝传递：

```python
async def execute_agent_task(self, task_id, target, scan_config):
    """执行AI Agent扫描任务"""
    initial_state = AgentState(
        target=target,
        task_id=str(task_id),
        target_context={**scan_config, 'strategy': strategy},
        user_tools=user_tools or selected_tools,
        user_requirement=user_requirement
    )

    # 依次执行4个子图
    agent_graph = ScanAgentGraph()
    state1 = await agent_graph.invoke_info_collection(initial_state)
    state2 = await agent_graph.invoke_vuln_scan(state1)
    state3 = await agent_graph.invoke_poc_verification(state2)
    final_state = await agent_graph.invoke_result_analysis(state3)
```

**（2）新版系统：3子图 + 意图识别 + interrupt中断**

TOSKill的AgentOrchestrator在旧版基础上增加了意图识别子图和interrupt交互机制：

```python
# 工作流暂停等待用户输入
user_choice = interrupt({
    "type": "interaction_required",
    "options": [
        {"key": "1", "label": "执行"},
        {"key": "2", "label": "停止"},
        {"key": "3", "label": "聊天"}
    ]
})

# 高危漏洞检测中断
user_decision = interrupt({
    "type": "high_risk_vulnerability_detected",
    "options": [
        {"key": "continue", "label": "继续扫描"},
        {"key": "stop", "label": "停止并报告"},
        {"key": "poc_verify", "label": "POC验证"}
    ]
})
```

**（3）LLM增强规划节点**

任务规划节点支持两种模式，LLM规划失败时自动降级为规则化规划：

```python
class TaskPlanningNode:
    async def _plan_tasks(self, state: AgentState) -> List[str]:
        if state.get("use_llm_planning"):
            try:
                # LLM增强规划：根据目标特征、用户需求动态规划
                prompt = self._build_planning_prompt(state)
                response = await self.llm.ainvoke(prompt)
                tasks = self._parse_planning_response(response)
                return tasks
            except Exception:
                # 自动降级为规则化规划
                pass

        # 规则化规划：根据CMS类型、端口信息选择默认任务
        return self._rule_based_planning(state)
```

### 5.2 RAG知识库增强检索实现

RAG模块基于LlamaIndex框架实现（仅新版系统），为AI Agent提供专业安全知识支持。

**知识库构建流程：**

```python
class TOSKillRAGEngine:
    """LlamaIndex RAG 引擎"""

    # 工具-知识映射表
    TOOL_KNOWLEDGE_MAP = {
        "sqli_scan":  ["SQL注入", "数据库攻击", "OWASP", "盲注", "Union注入"],
        "xss_scan":   ["XSS", "跨站脚本", "前端安全", "DOM型", "反射型"],
        "cmdi_scan":  ["命令注入", "RCE", "管道符", "命令执行"],
        "ssrf_scan":  ["SSRF", "云元数据", "内网访问", "协议利用"],
    }

    def __init__(self):
        # 使用HuggingFace嵌入模型进行向量化
        self.embed_model = HuggingFaceEmbedding(
            model_name="BAAI/bge-small-zh-v1.5"
        )
        self.index = self._build_index()

    def _build_index(self):
        documents = SimpleDirectoryReader(self.KNOWLEDGE_DIR).load_data()
        return VectorStoreIndex.from_documents(documents)

    def retrieve(self, query: str, tool_name: str = None) -> List[Document]:
        """根据查询和工具名检索相关知识"""
        if tool_name and tool_name in self.TOOL_KNOWLEDGE_MAP:
            keywords = self.TOOL_KNOWLEDGE_MAP[tool_name]
            query = f"{query} {' '.join(keywords)}"

        retriever = self.index.as_retriever(similarity_top_k=5)
        return retriever.retrieve(query)
```

**知识文档库涵盖14个专业主题**（详见第0.4节）。

### 5.3 插件执行引擎实现

**（1）插件多进程执行器（旧版）**

插件任务在独立进程中运行，通过心跳机制监控：

```python
class PluginExecutor:
    """插件多进程执行器"""

    def execute_plugin(self, plugin_name, target, config):
        process = multiprocessing.Process(
            target=self._run_plugin_in_process,
            args=(plugin_name, target, config, result_queue)
        )
        process.start()

        # 心跳监控
        while process.is_alive():
            if time.time() - last_heartbeat > heartbeat_timeout:
                process.terminate()
                raise PluginTimeoutError(f"插件 {plugin_name} 执行超时")
            time.sleep(1)

        return result_queue.get()
```

**（2）工具注册表实现**

```python
class ToolRegistry:
    """工具注册表"""

    _dangerous_patterns = {
        "rm -rf", "del /", "format", "shutdown",
        "drop table", "delete from", "truncate table",
        "<script>", "javascript:", "eval(",
        "exec(", "system(", "subprocess", "os.system"
    }

    async def call_tool(self, tool_name, target, use_cache=True, **kwargs):
        # 1. 安全检查
        security_result = self._security_check(tool_name, target, **kwargs)
        if not security_result["passed"]:
            return PluginResult.security_blocked(...)

        # 2. 缓存查询 (SHA256键 + TTL过期)
        if use_cache:
            cached = self._get_cached_result(cache_key)
            if cached: return cached

        # 3. 调用链追踪
        call_node = CallChainNode(tool_name=tool_name, ...)
        self._call_chain.append(call_node)

        # 4. 执行工具
        try:
            result = await tool.execute(target, **kwargs)
            self._update_tool_stats(tool_name, execution_time)
        except asyncio.TimeoutError:
            return PluginResult.timeout(...)
        except Exception as e:
            return PluginResult.failed(...)

        # 5. 缓存结果
        self._cache_result(cache_key, result, ttl=300)
        return result
```

**（3）新版系统工具注册（@tool装饰器模式）**

TOSKill使用LangChain的@tool装饰器注册工具，统一ToolResult格式：

```python
class ToolResult(TypedDict, total=False):
    success: bool
    data: Dict[str, Any]
    error: Optional[str]
    auth_info: Optional[Dict[str, Any]]
    timestamp: str

@tool
def baseinfo_scan(target: str) -> ToolResult:
    """基础信息收集工具"""
    # ... 实现逻辑
    return ToolResult(success=True, data={...}, error=None, ...)
```

**系统工具分类汇总：**

| 分类 | 旧版数量 | 新版数量 | 工具列表 |
|------|---------|---------|---------|
| 信息收集 | 14个 | 11个 | baseinfo, portscan, subdomain, dirscan, waf, cdnexist, whatcms, infoleak, webside, webweight, iplocating, loginfo, randheader (+ crawler旧版独有) |
| 漏洞扫描 | 9个 | 8个 | sqli, xss, csrf, ssrf, lfi, cmdi, fileupload, weakpass (+ infoleak旧版独有) |
| POC验证 | 14个 | 3个 | thinkphp, struts2, weblogic (新版) / 7大框架14个POC (旧版) |

### 5.4 WebSocket实时通信实现

**（1）后端ConnectionManager**

```python
class ConnectionManager:
    """WebSocket 连接管理器"""

    class ConnectionState(Enum):
        CONNECTING = "connecting"
        CONNECTED = "connected"
        DISCONNECTING = "disconnecting"
        DISCONNECTED = "disconnected"
        ERROR = "error"

    async def connect(self, websocket, client_host):
        if self._is_shutting_down:
            await websocket.close(code=1013)
            return
        await websocket.accept()
        self.connections[websocket] = ConnectionState.CONNECTED

    async def broadcast(self, message: Dict):
        disconnected = []
        for ws, state in self.connections.items():
            try:
                await ws.send_json(message)
            except Exception:
                disconnected.append(ws)
        for ws in disconnected:
            await self.disconnect(ws)

    async def close_all(self, reason="Server shutting down"):
        self._is_shutting_down = True
        for ws in list(self.connections.keys()):
            await ws.close(code=1001, reason=reason)
```

**（2）新版系统AIChatManager（TOSKill）**

TOSKill的WebSocket管理器增加了会话管理和工作流恢复功能：

```python
class AIChatManager:
    def __init__(self):
        self.sessions: Dict[str, Session] = {}       # 会话存储
        self.orchestrator = AgentOrchestrator()       # Agent编排器

    async def handle_message(self, ws, session_id, message):
        msg_type = message.get("type")
        if msg_type == "start_scan":
            await self.orchestrator.start_scan(session_id, message["payload"])
        elif msg_type == "user_confirm":
            # 恢复被interrupt暂停的工作流
            await self.orchestrator.resume_workflow(session_id, message["payload"])
        elif msg_type == "chat":
            await self.orchestrator.handle_chat(session_id, message["payload"])
```

**（3）前端WebSocketManager**

```javascript
class WebSocketManager {
  constructor() {
    this.messageQueue = []        // 离线消息队列
    this.messageHistory = []      // 消息历史 (最近100条)
    this.reconnectAttempts = 0
    this.maxReconnectAttempts = 5
  }

  sendAck(messageId) {
    this.send({ type: 'message_ack', payload: { message_id: messageId } })
  }

  // 重连策略 (指数退避 + 抖动)
  async reconnect() {
    const delay = Math.min(1000 * Math.pow(2, this.reconnectAttempts) + Math.random() * 1000, 30000)
    await new Promise(resolve => setTimeout(resolve, delay))
    await this.connect()
    this.flushMessageQueue()
  }
}
```

### 5.5 脚本安全审查实现

```python
class ScriptSafetyValidator:
    """脚本安全审查器"""

    DANGEROUS_FUNCTIONS = {
        'eval', 'exec', 'compile', '__import__',
        'os.system', 'os.popen', 'subprocess.call',
        'subprocess.Popen', 'subprocess.run',
        'socket.socket', 'requests.post',
        'shutil.rmtree', 'os.remove', 'os.unlink'
    }

    MALICIOUS_PATTERNS = [
        r'reverse_shell|反弹shell',
        r'os\.system\(.*nc\s',
        r'os\.system\(.*bash\s.*>&',
        r'base64\.b64decode',
        r'__import__\(.*pickle',
    ]

    def validate(self, script_content: str) -> Tuple[bool, str, Dict]:
        """多阶段安全审查"""
        # 阶段1: AST静态分析
        tree = ast.parse(script_content)
        dangerous_calls = self._find_dangerous_calls(tree)
        if dangerous_calls:
            return False, f"检测到危险函数调用: {dangerous_calls}", {}

        # 阶段2: 结构验证
        functions = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
        if 'run' not in functions:
            return False, "脚本缺少必需的 run(target) 函数", {}

        # 阶段3: 恶意模式检测
        for pattern in self.MALICIOUS_PATTERNS:
            if re.search(pattern, script_content, re.IGNORECASE):
                return False, f"检测到恶意模式: {pattern}", {}

        return True, "安全审查通过", {}
```

### 5.6 本章小结

本章详细阐述了系统的5大核心功能实现。AI Agent工作流引擎是平台的智能中枢，两套系统在实现上各有特色：旧版系统采用4子图顺序执行模式，新版系统采用3子图+意图识别+interrupt中断交互模式。RAG知识库（仅新版系统）为AI Agent提供专业安全知识增强，插件执行引擎（旧版）通过多进程隔离保障系统稳定性，WebSocket实时通信确保前后端双向数据同步，脚本安全审查通过多阶段AST分析保障自定义脚本的安全性。

---

## 6. 关键技术实现

### 6.1 LangGraph 多子图协同

**设计动机**：单一LangGraph图在工具数量多时容易触发递归限制（默认25），且不同阶段的逻辑耦合在一起难以维护。

**解决方案**：将完整工作流拆分为多个独立子图，每个子图独立编译、独立执行，子图间通过状态序列化/反序列化实现无缝传递。

**双系统对比：**

| 维度 | 旧版系统 | 新版系统 |
|------|---------|---------|
| 子图数量 | 4个 | 3个（+意图识别入口） |
| 子图类型 | 信息收集→漏洞扫描→POC验证→结果分析 | 意图识别→信息收集/漏洞扫描/报告 |
| 交互方式 | 顺序执行，无中断 | 支持interrupt中断，等待用户确认 |
| 递归限制 | 各子图独立(100/200) | 各子图独立 |
| 状态传递 | AgentState @dataclass to_dict/from_dict | ScanState TypedDict |

| 子图 | 递归限制 | 包含节点 | 职责 |
|------|---------|---------|------|
| 信息收集子图 | 100 | 环境感知→任务规划→工具执行→结果验证 | 端口扫描、子域名、CMS识别等 |
| 漏洞扫描子图 | 200 | 扫描规划→工具执行→结果汇总 | SQL注入、XSS、CSRF等 |
| POC验证子图 | 200 | POC规划→POC执行→结果验证 | 框架漏洞验证（仅旧版） |
| 结果分析子图 | 100 | 漏洞分析→报告生成 | 漏洞分析、AI增强报告 |

**优势**：
- 每个子图突破单一图的递归限制
- 阶段间可插入人工审核或条件判断
- 子图内部支持循环执行（工具执行→验证→继续执行）
- 每个子图可独立开发、测试和调试

### 6.2 LLM 增强动态任务规划

任务规划节点同时支持LLM增强规划和规则化规划两种模式，LLM规划失败时自动降级。

**LLM增强规划**：使用ChatOpenAI + 精心设计的Prompt模板，根据目标特征（CMS类型、端口、WAF等）、用户需求、执行历史智能规划任务序列。

**规则化规划**：基于预定义规则，根据CMS类型、端口信息选择默认任务。

**上下文感知工具选择**：

```
端口 3306 (MySQL)    → 推荐: sqli_scan, weakpass_scan
端口 6379 (Redis)    → 推荐: weakpass_scan
端口 80/443 (Web)    → 推荐: xss_scan, sqli_scan, dir_brute
端口 22 (SSH)        → 推荐: weakpass_scan
端口 3389 (RDP)      → 推荐: weakpass_scan
```

**自动回退（Fallback）机制**：

```
sqli_scan 失败 → 回退到 xss_scan, cmdi_scan
xss_scan 失败  → 回退到 csrf_scan
cmdi_scan 失败 → 回退到 lfi_scan
```

### 6.3 认证信息管理

新版系统（TOSKill）实现了认证信息的自动提取、加密存储和复用机制（旧版系统无此功能）：

```python
def encrypt_auth_info(auth_data: Dict[str, Any]) -> str:
    """加密认证信息"""
    json_str = json.dumps(auth_data, ensure_ascii=False)
    encoded = base64.b64encode(json_str.encode('utf-8')).decode('utf-8')
    return f"enc:{encoded}"

def decrypt_auth_info(encrypted_data: str) -> Dict[str, Any]:
    """解密认证信息"""
    encoded = encrypted_data.replace("enc:", "")
    json_str = base64.b64decode(encoded.encode('utf-8')).decode('utf-8')
    return json.loads(json_str)

def is_auth_expired(state: Dict[str, Any], default_expiry_minutes: int = 30) -> bool:
    """检查认证信息是否过期（默认30分钟）"""
    auth_timestamp = state.get("auth_timestamp")
    if auth_timestamp:
        expires_at = datetime.fromisoformat(auth_timestamp) + timedelta(minutes=default_expiry_minutes)
        return datetime.now() > expires_at
    return True
```

**认证生命周期管理**：
- **自动提取**：工具执行成功后，自动从响应中提取Cookies、Headers、Token
- **加密存储**：Base64编码加密存储，防止明文泄露
- **过期管理**：默认30分钟TTL，过期后自动标记为不可用
- **跨工具复用**：认证信息可在不同工具间共享（如weakpass扫描成功后，将cookie传递给sqli_scan）
- **重试机制**：认证失效时自动重试（最多3次），失败后回退到无认证模式

### 6.4 前端状态管理与可靠性

**（1）4层错误捕获架构**

```
┌──────────────────────────────────────┐
│ Layer 1: Vue errorHandler            │  app.config.errorHandler
│ Layer 2: Promise unhandledrejection  │  window.addEventListener
│ Layer 3: Window error                │  window.addEventListener
│ Layer 4: 网络状态监听                 │  offline/online events
└──────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│ GlobalErrorHandler                    │
│  - 8种网络错误模式匹配                │
│  - 自动重试 (最多3次, 递增延迟)       │
│  - 自定义事件派发 (global-error/retry) │
└──────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│ ErrorHandler (errorHandler.js)        │
│  - ErrorType: NETWORK/API/VALIDATION/ │
│    AUTH/UNKNOWN                       │
│  - AppError类: type+statusCode+details│
└──────────────────────────────────────┘
```

**（2）多重重试机制**

| 模块 | 重试次数 | 策略 | 触发条件 |
|------|---------|------|---------|
| aiAgents.js | 3次 | 指数退避 | 5xx/429/408及网络错误 |
| VulnerabilityDetailAPI | 3次 | 线性递增延迟 | 网络错误 (404不重试) |
| GlobalErrorHandler | 3次 | 递增延迟 | 网络错误自动重试 |
| WebSocket | 5次 | 指数退避+抖动 | 连接断开 |

**（3）前端构建优化**

```javascript
// vite.config.js 构建优化
build: {
  sourcemap: false,
  minify: 'terser',
  terserOptions: {
    compress: { drop_console: true, drop_debugger: true }
  },
  rollupOptions: {
    output: {
      manualChunks: {
        'vue-vendor': ['vue', 'vue-router', 'pinia'],
        'element-plus': ['element-plus'],
        'chart-js': ['chart.js'],
        'axios': ['axios']
      }
    }
  }
}
```

### 6.5 设计模式应用

系统在多个层面应用了经典设计模式，提升代码的可维护性和扩展性：

| 设计模式 | 应用场景 | 实现位置 | 说明 |
|---------|---------|---------|------|
| **模板方法模式** | 漏洞扫描器基类 | `backend/vulnerability_scan_plugins/base.py` | 定义scan()统一流程，子类实现build_payloads()和analyze_response() |
| **策略模式** | 任务规划 | `backend/ai_agents/core/nodes.py` | LLM规划策略与规则规划策略可动态切换，失败自动降级 |
| **适配器模式** | 工具适配器 | `backend/ai_agents/tools/adapters.py` | 将Plugin/POC/VulnScan三类工具统一适配为Agent可调用接口 |
| **装饰器模式** | 工具注册 | `TOSKill/AI/tools.py` | @tool装饰器将普通函数注册为LangChain工具 |
| **观察者模式** | WebSocket广播 | `backend/api/websocket.py` | ConnectionManager维护连接列表，任务状态变更时广播通知 |
| **单例模式** | 前端API服务 | `toskill-frontend/src/services/api.js` | ApiService单例，全局共享同一实例 |
| **状态模式** | 任务状态机 | `backend/models.py` | 严格状态转换校验，valid_transitions字典定义合法转换 |
| **工厂模式** | API响应 | `front/src/utils/apiResponse.js` | 12种工厂方法生成标准化API响应 |

### 6.6 本章小结

本章阐述了系统的关键技术实现。LangGraph多子图协同设计解决了单一图递归限制问题，LLM增强动态任务规划通过LLM+规则双模式实现智能决策，认证信息管理（TOSKill独有）实现了跨工具的认证共享，前端状态管理通过4层错误捕获和多重重试机制保障可靠性，此外系统在多个层面应用了模板方法、策略、适配器、观察者等经典设计模式。

---

## 7. 系统部署

### 7.1 部署架构设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                         生产部署架构                                  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                     Nginx (:80/:443)                           │ │
│  │                                                               │ │
│  │  location /              → front/dist/ (旧版前端SPA)           │ │
│  │  location /toskill/      → toskill-frontend/dist/ (新版前端)   │ │
│  │  location /api/          → 127.0.0.1:8888 (旧版后端)           │ │
│  │  location /ws            → 127.0.0.1:8888 (WebSocket)         │ │
│  │  location /health        → 127.0.0.1:8888 (健康检查)           │ │
│  └───────────────────────────────────────────────────────────────┘ │
│         │                    │                                      │
│         ▼                    ▼                                      │
│  ┌──────────────────┐  ┌──────────────────────────────────────┐   │
│  │ 静态文件服务       │  │ 旧版后端 (Uvicorn)                     │   │
│  │ front/dist/       │  │ 127.0.0.1:8888                       │   │
│  │ toskill-frontend/ │  │                                      │   │
│  │ dist/             │  │ supervisor / systemd 管理进程          │   │
│  └──────────────────┘  │                                      │   │
│                        │ 数据目录:                              │   │
│                        │ data/webscan.db (SQLite)              │   │
│                        │ logs/app.log                          │   │
│                        │ logs/api.log                          │   │
│                        │ reports/ (报告文件)                    │   │
│                        │ uploads/ (上传文件)                    │   │
│                        └──────────────────────────────────────┘   │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 新版后端 (可选独立部署)                                        │  │
│  │ 127.0.0.1:8081                                               │  │
│  │                                                              │  │
│  │ 独立运行模式: python TOSKill/main.py                          │  │
│  │ 嵌入模式: 由旧版后端 import 引入，共享8888端口                  │  │
│  │                                                              │  │
│  │ 数据目录:                                                     │  │
│  │ data/toskill.db (SQLite)                                     │  │
│  │ logs/toskill.log                                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.2 后端部署流程

**旧版后端部署：**

```bash
# 1. 安装依赖
cd backend
pip install -r requirements.txt

# 2. 配置环境变量 (.env 文件)
# APP_NAME=WebScan AI Security Platform
# HOST=127.0.0.1
# PORT=8888
# DATABASE_URL=sqlite://data/webscan.db
# OPENAI_API_KEY=your_key
# AWVS_API_URL=https://127.0.0.1:3443
# AWVS_API_KEY=your_awvs_key

# 3. 初始化数据库
python -c "from backend.database import init_db; import asyncio; asyncio.run(init_db())"

# 4. 启动服务
python -m backend.main
# 服务监听 http://127.0.0.1:8888
# API文档 http://127.0.0.1:8888/docs
```

**新版后端部署（独立模式）：**

```bash
# 1. 安装依赖
cd TOSKill
pip install -r requirements.txt

# 2. 配置环境变量 (.env 文件)
# APP_NAME=TOSKill Security Scanner
# HOST=127.0.0.1
# PORT=8081
# OPENAI_API_KEY=your_key
# OPENAI_BASE_URL=https://maas-api.cn-huabei-1.xf-yun.com/v2
# MODEL_ID=xop35qwen2b

# 3. 启动服务
python main.py
# 服务监听 http://127.0.0.1:8081
# API文档 http://127.0.0.1:8081/docs
```

**Gunicorn生产部署（推荐）：**

```bash
pip install gunicorn
gunicorn TOSKill.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 127.0.0.1:8081
```

### 7.3 前端部署流程

#### 旧版前端部署

```bash
# 1. 安装依赖
cd front
npm install

# 2. 生产构建
npm run build
# 输出到 front/dist/

# 3. Nginx 配置
# 将 front/dist/ 目录配置为静态文件根目录
```

**构建优化特性：**
- 代码分割：vue-vendor / element-plus / chart-js / axios 独立chunk
- Terser压缩：drop_console + drop_debugger
- 无SourceMap：生产环境不暴露源码
- ChunkSizeWarningLimit：500KB

#### 新版前端部署

```bash
# 1. 安装依赖
cd toskill-frontend
npm install

# 2. 生产构建
npm run build
# 输出到 toskill-frontend/dist/

# 3. Nginx 配置
# 将 toskill-frontend/dist/ 配置为 /toskill/ 路径
```

**部署模式对比：**

| 部署维度 | 旧版前端 | 新版前端 |
|---------|---------|---------|
| 源文件数 | ~50个 | ~22个 |
| 构建产物大小 | 较大（含Element Plus/Chart.js） | 较小（仅Vue 3 + marked + Lucide Icons） |
| 依赖数量 | 6个运行时 + 7个开发依赖 | 3个运行时 + 2个开发依赖 |
| Nginx路径 | `/`（主入口） | `/toskill/`（子路径） |
| API代理目标 | `:8888`（旧版后端，含TOSKill路由） | `:8081`（TOSKill独立后端） |

### 7.4 本章小结

本章阐述了系统的部署架构与流程。部署采用Nginx反向代理统一入口，旧版后端(8888端口)作为主力服务同时承载旧版API和TOSKill路由，新版后端(8081端口)作为独立可选服务。前端部署支持双端口开发模式（旧版前端→8888，新版前端→8081），生产环境通过Nginx统一路由。两套系统在部署流程上保持独立性，互不干扰，可根据实际需要灵活选择嵌入模式或独立模式部署TOSKill。

---

## 8. 系统界面展示

### 8.1 旧版系统界面（Classic WebScan）

旧版系统基于Element Plus企业级组件库，提供完整的Web扫描管理界面，共15个功能页面。

**（1）仪表盘首页（Dashboard）**

```
┌──────────────────────────────────────────────────────────────────┐
│  [侧边栏]  [WebScan AI Security Platform]          [通知] [用户]  │
├───────────┬──────────────────────────────────────────────────────┤
│  📊 仪表盘  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  📋 任务管理 │  │ 扫描任务   │ │ 发现漏洞   │ │ 生成报告   │ │ 活跃用户 │ │
│  🔍 POC扫描 │  │   128     │ │   256     │ │   89      │ │   12   │ │
│  🤖 Agent  │  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
│  🔌 AWVS   │                                                      │
│  🛡️ 漏洞结果 │  ┌─────────────────────────────────────────────────┐│
│  📄 报告管理 │  │  扫描趋势图 (Chart.js 折线图)                     ││
│  📚 知识库   │  │  ████░░░░░░░░  一月  45次扫描                    ││
│  ⚙️ 设置    │  │  ██████░░░░░░  二月  78次扫描                    ││
│  🔔 通知    │  │  ██████████░░  三月  112次扫描                   ││
│              │  └─────────────────────────────────────────────────┘│
│              │  ┌──────────────────────┐ ┌──────────────────────┐ │
│              │  │ 最近任务 (WebSocket   │ │ 漏洞严重度分布        │ │
│              │  │  实时更新)            │ │ (饼图: 高/中/低/信息) │ │
│              │  └──────────────────────┘ └──────────────────────┘ │
└──────────────┴──────────────────────────────────────────────────────┘
```

**（2）AI Agent扫描页面（AgentScan）**

```
┌──────────────────────────────────────────────────────────────────┐
│  AI Agent 智能扫描                                                 │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  目标URL: [https://example.com        ]  [开始扫描]           │ │
│  │  扫描策略: [标准扫描 ▼]  并发数: [5]  超时: [300s]            │ │
│  │  选择工具: ☑基础信息 ☑端口扫描 ☑SQL注入 ☑XSS ☑弱口令       │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────┐  ┌────────────────────────────┐ │
│  │  工作流进度 (WorkflowTimeline)│  │  AI对话面板 (AIChatPanel)  │ │
│  │                             │  │                            │ │
│  │  ● 信息收集 ───────── 100%  │  │  [AI]: 已识别目标为        │ │
│  │  ● 漏洞扫描 ─────────  75%  │  │  Apache服务器，PHP 7.4    │ │
│  │  ◌ POC验证  ─────────   0%  │  │  正在规划扫描策略...      │ │
│  │  ◌ 结果分析 ─────────   0%  │  │                            │ │
│  │                             │  │  [用户]: 重点关注SQL注入   │ │
│  │  子图进度:                  │  │  [AI]: 好的，我将优先      │ │
│  │  ████████░░  8/10 任务完成  │  │  执行SQL注入检测           │ │
│  └─────────────────────────────┘  └────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  实时日志 (WebSocket)                                         │ │
│  │  [12:30:01] 开始执行: baseinfo_scan                          │ │
│  │  [12:30:05] baseinfo_scan 完成，发现Apache/2.4.41            │ │
│  │  [12:30:06] 开始执行: portscan (端口 1-1000)                 │ │
│  │  [12:31:20] portscan 完成，开放端口: 80, 443, 3306           │ │
│  │  [12:31:21] AI决策: 检测到MySQL端口3306，推荐执行sqli_scan   │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

**（3）漏洞结果页面（VulnerabilityResults）**

```
┌──────────────────────────────────────────────────────────────────┐
│  漏洞结果                                       [导出] [筛选 ▼]   │
│                                                                   │
│  [结果列表] [执行日志]                                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🔴 高危  SQL注入漏洞                          风险评分: 8.5  │ │
│  │ https://example.com/api/users?id=1                            │ │
│  │ 发现时间: 2026-06-15 14:30:22   状态: 未修复                  │ │
│  │ [查看详情] [下载POC] [标记已修复]                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🟡 中危  XSS跨站脚本漏洞                       风险评分: 5.2  │ │
│  │ https://example.com/search?q=test                             │ │
│  │ 发现时间: 2026-06-15 14:32:10   状态: 未修复                  │ │
│  │ [查看详情] [下载POC] [标记已修复]                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🟢 低危  信息泄露                            风险评分: 2.1   │ │
│  │ https://example.com/.git/config                               │ │
│  │ 发现时间: 2026-06-15 14:28:05   状态: 未修复                  │ │
│  │ [查看详情] [标记已修复] [忽略]                                 │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 8.2 新版系统界面（TOSKill）

新版系统采用轻量级原生CSS设计，以AI对话式交互为核心，共5个功能视图。

**（1）AI对话控制台（ConsoleView）— 核心交互界面**

```
┌──────────────────────────────────────────────────────────────────┐
│  [TOSKill]  [☰ 控制台] [扫描] [工具] [报告] [设置]    ● 已连接   │
├────────────┬─────────────────────────────────────────────────────┤
│  历史会话   │                                                     │
│            │  ┌─────────────────────────────────────────────────┐│
│  ● 扫描    │  │  🤖 AI助手                                       ││
│    example │  │  欢迎使用 TOSKill AI安全扫描平台！               ││
│    .com    │  │  请输入目标URL开始扫描，或描述您的安全需求。      ││
│            │  │                                                 ││
│  ● 漏洞分析 │  │  ───────────────────────────────────────────── ││
│    2026-06 │  │                                                 ││
│            │  │  👤 用户                                        ││
│  ● 安全咨询 │  │  请对 https://example.com 进行全面安全扫描      ││
│            │  │                                                 ││
│  [+ 新会话] │  │  ───────────────────────────────────────────── ││
│            │  │                                                 ││
│            │  │  🤖 AI助手                                       ││
│            │  │  好的，我将对 example.com 进行完整扫描。          ││
│            │  │  首先进行信息收集...                              ││
│            │  │                                                 ││
│            │  │  ┌──────────────────────────────────────┐       ││
│            │  │  │ ⚡ 工作流进度                          │       ││
│            │  │  │ 信息收集 ████████████ 100%           │       ││
│            │  │  │ 漏洞扫描 ██████░░░░░░  60%           │       ││
│            │  │  │ 报告生成 ░░░░░░░░░░░░   0%           │       ││
│            │  │  └──────────────────────────────────────┘       ││
│            │  │                                                 ││
│            │  │  ⚠️ 检测到高危漏洞，需要您的确认：               ││
│            │  │  [1. 继续扫描] [2. 停止并报告] [3. POC验证]     ││
│            │  └─────────────────────────────────────────────────┘│
│            │                                                     │
│            │  ┌─────────────────────────────────────────────────┐│
│            │  │  > [输入命令或消息...]                    [发送]  ││
│            │  └─────────────────────────────────────────────────┘│
└────────────┴─────────────────────────────────────────────────────┘
│  当前目标: https://example.com          [浮动日志面板 📋]         │
└──────────────────────────────────────────────────────────────────┘
```

**（2）扫描视图（ScanView）**

```
┌──────────────────────────────────────────────────────────────────┐
│  扫描任务                                                         │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  目标URL: [https://example.com        ]                      │ │
│  │  扫描模式: [完整扫描 ▼]  [快速扫描] [深度扫描] [信息收集]    │ │
│  │  [开始扫描]                                                   │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  扫描进度                                                     │ │
│  │  ████████████░░░░░░░░  60%                                   │ │
│  │  已完成: 6/10 任务                                            │ │
│  │                                                               │ │
│  │  ✅ baseinfo_scan     完成 (2.3s)                             │ │
│  │  ✅ portscan          完成 (15.8s) - 开放端口: 80,443,3306   │ │
│  │  ✅ waf_detect        完成 (1.2s) - 未检测到WAF               │ │
│  │  ✅ cms_detect        完成 (0.8s) - WordPress 6.2             │ │
│  │  🔄 sqli_scan         执行中...                               │ │
│  │  ⏳ xss_scan           等待中                                  │ │
│  │  ⏳ cmdi_scan          等待中                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

**（3）工具列表视图（ToolsView）**

```
┌──────────────────────────────────────────────────────────────────┐
│  安全工具列表                                        [搜索工具...] │
│                                                                   │
│  [全部] [信息收集] [漏洞扫描] [POC验证] [报告生成]                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🔍 portscan          端口扫描与服务识别             ⏱ 120s   │ │
│  │   检测目标主机的开放端口和运行服务                             │ │
│  │   [执行工具]                                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🔍 subdomain         子域名发现                     ⏱ 120s   │ │
│  │   发现目标域名的子域名资产                                     │ │
│  │   [执行工具]                                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🛡️ sqli_scan         SQL注入扫描                    ⏱ 180s   │ │
│  │   报错注入 + 时间盲注 + 布尔盲注 + Union注入                    │ │
│  │   风险等级: 🔴 高危                                           │ │
│  │   [执行工具]                                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 🛡️ xss_scan          XSS跨站脚本扫描                ⏱ 120s   │ │
│  │   反射型 + 存储型 + DOM型 XSS检测                              │ │
│  │   风险等级: 🟡 中危                                           │ │
│  │   [执行工具]                                                  │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

**界面设计对比：**

| 设计维度 | 旧版系统 (Classic WebScan) | 新版系统 (TOSKill) |
|---------|--------------------------|-------------------|
| **设计风格** | 企业级管理后台 | 极简AI对话式 |
| **UI框架** | Element Plus（重型组件库） | 原生CSS + Lucide Icons |
| **导航方式** | 侧边栏 + 多级路由 | 顶部导航 + 单页视图切换 |
| **主交互模式** | 表单填写 + 列表管理 | AI对话 + 命令输入 |
| **页面数量** | 15个独立页面 | 5个功能视图 + Agent工作空间 |
| **实时反馈** | 多WebSocket分区更新 | 单WebSocket全流程驱动 |
| **AI集成** | 独立AI面板（辅助） | AI对话为主交互入口（核心） |
| **移动端适配** | 响应式CSS变量 | 轻量布局，天然适配 |

### 8.3 本章小结

本章通过ASCII界面示意图展示了双系统的UI设计差异。旧版系统采用Element Plus企业级组件库，提供15个功能页面，覆盖仪表盘、任务管理、漏洞查看、报告导出等完整的管理后台功能，适合专业安全人员的批量扫描管理场景。新版系统采用极简AI对话式设计，以5个功能视图和Agent工作空间构建核心交互，强调自然语言驱动的实时扫描体验，降低了使用门槛。两种界面设计风格分别服务于不同的用户场景：旧版面向专业批量运维，新版面向探索式AI交互。

---

## 附录

### A. 项目技术栈全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        WebScan AI Security Platform                           │
│                            技术栈全景图                                       │
│                                                                               │
│  前端层                                                                       │
│  ┌──────────────────────────────┐  ┌──────────────────────────────┐         │
│  │ 旧版前端 (front/)             │  │ 新版前端 (toskill-frontend/)  │         │
│  │ Vue 3.5.24 + Vite 5.4.0      │  │ Vue 3.5.32 + Vite 8.0.10     │         │
│  │ Element Plus 2.8.0           │  │ 原生CSS + Lucide Icons 1.0   │         │
│  │ Pinia 2.3.0 + Vue Router     │  │ Reactive Store (单文件)       │         │
│  │ Axios 1.7.9 + Chart.js 4.4   │  │ Fetch API + marked 18.0      │         │
│  │ ~50个源文件                    │  │ ~22个源文件                   │         │
│  └──────────────┬───────────────┘  └──────────────┬───────────────┘         │
│                 │                                  │                          │
│  后端层         ▼                                  ▼                          │
│  ┌──────────────────────────────┐  ┌──────────────────────────────┐         │
│  │ 旧版后端 (backend/)           │  │ 新版后端 (TOSKill/)           │         │
│  │ FastAPI (8888端口)            │  │ FastAPI (8081端口)            │         │
│  │ Tortoise-ORM 0.21.6          │  │ 内存状态 + 可选SQLite         │         │
│  │ LangGraph (4子图)             │  │ LangGraph (3子图+意图识别)    │         │
│  │ LangChain + ChatOpenAI        │  │ LangChain + LlamaIndex       │         │
│  │ ~80个源文件                    │  │ ~35个源文件                   │         │
│  └──────────────┬───────────────┘  └──────────────┬───────────────┘         │
│                 │                                  │                          │
│  数据层         ▼                                  ▼                          │
│  ┌──────────────────────────────┐  ┌──────────────────────────────┐         │
│  │ SQLite (webscan.db)          │  │ SQLite (toskill.db, 可选)     │         │
│  │ 14张数据表                    │  │ 会话存储 (TTL过期清理)        │         │
│  │ 严格状态机校验                │  │ LangGraph MemorySaver         │         │
│  └──────────────────────────────┘  └──────────────────────────────┘         │
│                                                                               │
│  AI/ML层                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │ LangGraph (工作流引擎) + LangChain (LLM框架) + LlamaIndex (RAG, 仅新版) │    │
│  │ ChatOpenAI (LLM API) + HuggingFace Embedding (BAAI/bge-small-zh-v1.5) │    │
│  │ 14个RAG知识文档 (仅新版) + ReACT推理决策模式                            │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  部署层                                                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │ Nginx (反向代理) + Uvicorn (ASGI) + Supervisor/systemd (进程管理)      │    │
│  │ Docker (可选) + 日志系统 (JSON格式 + 文件轮转)                         │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

### B. 代码规模统计

| 系统 | 组件 | 源文件数 | 代码行数(约) | 说明 |
|------|------|---------|------------|------|
| 旧版 | front/ | ~50 | ~8,000 | Vue 3 SPA，15页面，13业务组件 |
| 旧版 | backend/ | ~80 | ~15,000 | FastAPI，17个API模块，14张表 |
| 新版 | toskill-frontend/ | ~22 | ~3,000 | Vue 3 SPA，5视图，Agent工作空间 |
| 新版 | TOSKill/ | ~35 | ~8,000 | FastAPI，6个API模块，LangGraph工作流 |
| 共享 | TOSKill/tools/ | ~25 | ~6,000 | 22个安全工具，双系统共享 |
| 共享 | backend/poc/ | ~14 | ~3,000 | POC脚本，7大框架 |
| **合计** | | **~226** | **~43,000** | |

### C. 关键依赖版本

| 依赖 | 旧版系统 | 新版系统 | 用途 |
|------|---------|---------|------|
| Python | 3.8+ | 3.8+ | 运行环境 |
| FastAPI | 0.115+ | 0.115+ | Web框架 |
| Uvicorn | 0.34+ | 0.34+ | ASGI服务器 |
| Pydantic | 2.10+ | 2.10+ | 数据验证 |
| LangChain | 0.3+ | 0.3+ | LLM框架 |
| LangGraph | 0.2+ | 0.2+ | AI工作流 |
| LlamaIndex | - | latest | RAG知识库 |
| Tortoise-ORM | 0.21.6 | - | 数据库ORM |
| SQLite | ✓ | ✓ (可选) | 数据库 |
| Vue | 3.5.24 | 3.5.32 | 前端框架 |
| Vite | 5.4.0 | 8.0.10 | 构建工具 |
| Element Plus | 2.8.0 | - | UI组件库 |
| Pinia | 2.3.0 | - | 状态管理 |
| Vue Router | 4.5.24 | - | 路由管理 |
| Axios | 1.7.9 | - | HTTP客户端 |
| Chart.js | 4.4.0 | - | 图表库 |
| marked | - | 18.0.3 | Markdown渲染 |
| lucide-vue-next | - | 1.0.0 | 图标库 |

### D. 端口规划汇总

| 服务 | 端口 | 协议 | 运行模式 | 说明 |
|------|------|------|---------|------|
| Nginx | 80/443 | HTTP/HTTPS | 生产 | 统一入口，反向代理 |
| 旧版后端 | 8888 | HTTP | 开发/生产 | FastAPI + Uvicorn，内置TOSKill路由 |
| 新版后端 | 8081 | HTTP | 开发/生产(可选) | TOSKill独立运行 |
| 旧版前端(Dev) | 5173 | HTTP | 开发 | Vite Dev Server，代理→8888 |
| 新版前端(Dev) | 5173 | HTTP | 开发 | Vite Dev Server，代理→8081 |

> 注：两个前端开发服务器不能同时以默认端口5173启动，需先后启动或修改端口配置。

### E. 双系统架构演进总结

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          双系统架构演进总结                                    │
│                                                                               │
│  维度          旧版系统 (Classic WebScan)     新版系统 (TOSKill)               │
│  ───────────  ──────────────────────────  ──────────────────────────────     │
│  定位          生产主力，功能完整              创新探索，AI原生                  │
│  设计哲学       传统Web管理后台                 AI对话式交互                     │
│  前端规模       ~50个源文件，15个页面           ~22个源文件，5个视图             │
│  后端规模       ~80个源文件，17个API模块        ~35个源文件，6个API模块          │
│  数据持久化     SQLite + Tortoise-ORM (14张表)  内存状态 + 可选SQLite            │
│  AI集成        LangGraph 4子图 (顺序执行)       LangGraph 3子图+意图识别 (中断)  │
│  RAG支持       无                              LlamaIndex (14个知识文档)        │
│  通信方式       REST为主 + WebSocket辅助        WebSocket为主 + REST辅助         │
│  外部集成       AWVS + Seebug + Pocsuite3      无                              │
│  用户交互       批量创建任务，异步执行            实时对话式，interrupt中断确认     │
│  认证管理       无                              认证信息自动提取/加密/共享        │
│  部署模式       必选（核心服务）                  可选（独立或嵌入）               │
│                                                                               │
│  演进路径:  Classic WebScan → 引入AI Agent → TOSKill重构 → 双系统并行 → 统一架构 │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

*本文档基于 WebScan AI Security Platform 项目实际代码结构编写，所有架构图、流程图、数据表结构均来源于实际代码分析。文档版本: v2.0，最后更新日期: 2026-07-01。*