# WebScan AI - Web应用漏洞扫描智能体前端

基于轻量化大模型的Web应用漏洞扫描智能体前端界面，采用Vue 3 + Vite构建。

## 功能特性

- 🎯 **专业简洁的界面设计** - 符合安全工具的专业形象
- 📊 **智能仪表盘** - 实时显示扫描统计和漏洞趋势
- 🔍 **扫描任务管理** - 创建、监控和管理扫描任务
- 🚨 **漏洞结果展示** - 按优先级展示漏洞，支持筛选和搜索
- 🤖 **AI分析集成** - 智能分析漏洞风险和修复建议
- 📋 **报告生成** - 支持HTML、PDF、JSON多种格式
- ⚙️ **系统设置** - 灵活的配置选项
- 📱 **响应式设计** - 适配桌面、平板和手机设备

## 技术栈

- **Vue 3** - 渐进式JavaScript框架
- **Vue Router** - 官方路由管理器
- **Vite** - 下一代前端构建工具
- **CSS3** - 现代CSS特性和变量系统

## 项目结构

```
front/
├── src/
│   ├── components/          # 公共组件
│   │   └── AppLayout.vue   # 主布局组件
│   ├── views/              # 页面组件
│   │   ├── Dashboard.vue   # 仪表盘
│   │   ├── ScanTasks.vue   # 扫描任务管理
│   │   ├── VulnerabilityResults.vue  # 漏洞结果
│   │   ├── VulnerabilityDetail.vue   # 漏洞详情
│   │   ├── Reports.vue     # 报告生成
│   │   └── Settings.vue    # 系统设置
│   ├── data/               # 示例数据
│   │   └── mockData.js     # 模拟数据文件
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── style.css          # 全局样式
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── docs/                   # 文档
│   └── API.md             # API接口文档
├── package.json           # 项目配置
├── README.md             # 项目说明
└── index.html            # HTML模板
```

## 快速开始

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 预览生产版本
```bash
npm run preview
```

## 示例数据说明

项目包含完整的示例数据用于前端开发和测试，所有数据都在 `src/data/mockData.js` 文件中定义。

### 数据类型

- **扫描任务数据** (`mockScanTasks`) - 包含任务状态、优先级、进度等
- **漏洞数据** (`mockVulnerabilities`) - 详细的漏洞信息和技术细节
- **仪表盘统计** (`mockDashboardStats`) - 统计数据和趋势信息
- **AI分析数据** (`mockAIAnalysis`) - 智能分析结果和建议
- **报告数据** (`mockReports`) - 历史报告列表
- **系统设置** (`mockSettings`) - 配置选项和规则
- **通知数据** (`mockNotifications`) - 系统通知信息
- **用户信息** (`mockUserInfo`) - 用户账户信息

### TODO标识说明

前端代码中所有需要对接后端API的地方都标记了 `TODO` 注释，格式如下：

```javascript
// TODO: 替换为真实的API调用
// TODO: 从API获取数据 - GET /api/endpoint
```

这些标识帮助开发者快速定位需要替换为真实API调用的位置。

## API接口文档

详细的API接口文档请查看 [docs/API.md](./docs/API.md)，包含：

### 主要接口分类

1. **认证接口** - 用户登录、token管理
2. **仪表盘接口** - 统计数据、趋势图表
3. **扫描任务接口** - CRUD操作、状态管理
4. **漏洞管理接口** - 漏洞列表、详情、AI分析
5. **报告管理接口** - 生成、下载、管理报告
6. **系统设置接口** - 配置管理、规则设置
7. **通知接口** - 消息推送、状态更新
8. **用户管理接口** - 个人信息、权限管理

### API调用示例

```javascript
// 获取仪表盘数据
const response = await fetch('/api/dashboard/stats', {
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

### WebSocket支持

系统支持WebSocket实时通信，用于：
- 扫描进度实时更新
- 漏洞发现即时通知
- 系统状态变化推送

## 色彩系统

| 类型 | 颜色代码 | 用途 | CSS变量 |
|------|----------|------|---------|
| 主色 | `#1a3a6c` | 标题、关键按钮 | `--primary-color` |
| 辅助色 | `#4a90e2` | 操作按钮、链接 | `--secondary-color` |
| 高危 | `#e74c3c` | 高危漏洞标识 | `--high-risk` |
| 中危 | `#f5a623` | 中危漏洞标识 | `--medium-risk` |
| 低危 | `#f1c40f` | 低危漏洞标识 | `--low-risk` |
| 成功 | `#2ecc71` | 成功状态 | `--success-color` |
| 背景 | `#f8f9fa` | 主背景色 | `--background-color` |
| 卡片 | `#ffffff` | 卡片背景 | `--card-background` |

## 页面功能详解

### 1. 仪表盘 (Dashboard)
- **数据卡片**: 今日扫描、高危漏洞、趋势变化、完成任务
- **趋势图表**: 7/30/90天漏洞发现趋势
- **最新结果**: 最近4个扫描任务状态
- **快速操作**: 一键跳转到主要功能

**TODO位置**:
```javascript
// TODO: 从API获取仪表盘统计数据 - GET /api/dashboard/stats
// TODO: 从API获取趋势数据 - GET /api/dashboard/trends?period=7
// TODO: 从API获取最新扫描结果 - GET /api/scans/recent?limit=4
```

### 2. 扫描任务管理 (ScanTasks)
- **任务列表**: 支持状态、优先级筛选
- **创建任务**: 多URL支持、高级配置选项
- **状态监控**: 实时进度显示
- **批量操作**: 停止、重启、删除任务

**TODO位置**:
```javascript
// TODO: 从API获取扫描任务列表 - GET /api/scan-tasks
// TODO: 调用API创建扫描任务 - POST /api/scan-tasks
```

### 3. 漏洞结果展示 (VulnerabilityResults)
- **优先级展示**: 星级+颜色双重标识
- **多维筛选**: 类型、状态、关键词搜索
- **AI分析摘要**: 风险评估和关键发现
- **状态管理**: 标记修复、误报

**TODO位置**:
```javascript
// TODO: 从API获取任务信息 - GET /api/scan-tasks/{taskId}
// TODO: 从API获取漏洞统计 - GET /api/scan-tasks/{taskId}/vulnerability-stats
// TODO: 从API获取漏洞列表 - GET /api/scan-tasks/{taskId}/vulnerabilities
```

### 4. 漏洞详情 (VulnerabilityDetail)
- **基本信息**: CVSS评分、影响范围、发现位置
- **技术细节**: 漏洞描述、载荷示例、响应内容
- **AI分析**: 优先级理由、风险关联、误报判断
- **修复建议**: 分步骤修复指导和代码示例

**TODO位置**:
```javascript
// TODO: 从API获取漏洞详情 - GET /api/vulnerabilities/{id}
// TODO: 从API获取AI分析 - GET /api/vulnerabilities/{id}/ai-analysis
// TODO: 从API获取修复步骤 - GET /api/vulnerabilities/{id}/fix-steps
```

### 5. 报告生成 (Reports)
- **格式选择**: HTML、PDF、JSON多种格式
- **内容定制**: 摘要、漏洞详情、修复建议等
- **实时预览**: 报告内容预览
- **历史管理**: 下载、删除历史报告

**TODO位置**:
```javascript
// TODO: 从API获取扫描任务列表 - GET /api/scan-tasks
// TODO: 从API获取报告历史 - GET /api/reports
```

### 6. 系统设置 (Settings)
- **常规设置**: 系统名称、语言、时区
- **扫描配置**: 默认参数、规则管理
- **通知设置**: 邮件配置、事件订阅
- **安全设置**: 会话管理、API密钥

**TODO位置**:
```javascript
// TODO: 从API获取系统设置 - GET /api/settings
// TODO: 从API获取扫描规则 - GET /api/scan-rules
// TODO: 从API获取API密钥 - GET /api/api-keys
```

## 开发指南

### 替换示例数据步骤

1. **安装HTTP客户端**
```bash
npm install axios
```

2. **创建API服务文件**
```javascript
// src/services/api.js
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:8080/api',
  headers: {
    'Content-Type': 'application/json'
  }
})

export default api
```

3. **替换组件中的TODO**
```javascript
// 替换前
import { mockDashboardStats } from '../data/mockData.js'
todayScans: mockDashboardStats.todayScans,

// 替换后
import api from '../services/api.js'
async mounted() {
  const response = await api.get('/dashboard/stats')
  this.todayScans = response.data.todayScans
}
```

### 状态管理建议

对于复杂的状态管理，建议使用Pinia：

```bash
npm install pinia
```

```javascript
// stores/scan.js
import { defineStore } from 'pinia'

export const useScanStore = defineStore('scan', {
  state: () => ({
    tasks: [],
    currentTask: null
  }),
  actions: {
    async fetchTasks() {
      // API调用
    }
  }
})
```

## 部署说明

### 环境变量配置

创建 `.env` 文件：
```bash
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws
```

### 生产构建

```bash
npm run build
```

构建产物在 `dist/` 目录，可直接部署到静态服务器。

### Docker部署

```dockerfile
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
EXPOSE 80
```

## 测试

### 单元测试
```bash
npm install @vue/test-utils vitest -D
npm run test
```

### E2E测试
```bash
npm install cypress -D
npm run test:e2e
```

## 性能优化

- **代码分割**: 路由级别的懒加载
- **资源压缩**: Vite自动处理CSS/JS压缩
- **缓存策略**: API响应缓存和本地存储
- **图片优化**: WebP格式和懒加载

## 安全考虑

- **XSS防护**: 所有用户输入都经过转义
- **CSRF保护**: API请求包含CSRF令牌
- **内容安全策略**: 配置CSP头部
- **敏感信息**: 不在前端存储敏感数据

## 浏览器支持

| 浏览器 | 最低版本 | 说明 |
|--------|----------|------|
| Chrome | 88+ | 完全支持 |
| Firefox | 85+ | 完全支持 |
| Safari | 14+ | 完全支持 |
| Edge | 88+ | 完全支持 |
| IE | - | 不支持 |

## 贡献指南

1. Fork项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建Pull Request

## 更新日志

### v1.0.0 (2024-11-17)
- ✨ 初始版本发布
- 🎨 完整的UI设计系统
- 📊 仪表盘和数据可视化
- 🔍 扫描任务管理功能
- 🚨 漏洞结果展示和详情
- 📋 报告生成和管理
- ⚙️ 系统设置和配置
- 🤖 AI分析集成
- 📱 响应式设计支持

## 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 联系方式

- 项目地址: https://github.com/webscan-ai/frontend
- 问题反馈: https://github.com/webscan-ai/frontend/issues
- 邮箱: support@webscan.ai

---

**注意**: 本项目目前使用模拟数据，所有标记为 `TODO` 的地方都需要替换为真实的后端API调用。详细的API接口文档请参考 [docs/API.md](./docs/API.md)。
